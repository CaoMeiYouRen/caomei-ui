# 治理决策与经验归档

本目录记录 caomei-ui 的跨模块 / 治理 / 重大变更专项设计，以及可复用的经验教训。

## 命名约定

- 决策类：`[YYYY-MM-DD]-<topic>.md`（如 `2026-09-12-primevue-migration-evaluation.md`）。
- 经验类：`experience-archive.md`（按需拆分）。
- 通用治理：`<topic>-governance.md`。

## 收录标准

- 改动预计 > 10 文件 / > 800 行，或跨 ≥ 2 个模块；
- 引入新依赖、外部 skill / agent / MCP；
- 重大技术选型或架构变更；
- 需要跨项目复用的经验教训；
- 阶段范围 / 发版就绪度评估（阶段启动前置或阶段内分段）。

## 当前条目

- [2026-09-25-momei-upstream-feedback-evaluation.md](./2026-09-25-momei-upstream-feedback-evaluation.md)：momei 上游反馈评估与 Backlog 登记。**触发**：momei 仓库 `docs/design/governance/2026-09-25-caomei-ui-upstream-feedback.md`（PrimeVue → caomei-ui 迁移沉淀；8 条待反馈项 + 6 条「非问题」）。**事实源**：momei `f578ea71`（完整 40 位 revision 与工作区脏状态见记录文首，只读该文件）+ 本仓 `0.3.0`（反馈自述 0.2.0，差异为 Phase 13 增量、与 8 条受检项不相交）。**方法**：只读静态比对，零 `src/` 改动。**结论**：8 条受检项中**真实缺口 3**（Button `iconOnly` / DataTable 分页对齐 token / Select `#value` 插槽）、**文档缺口 3**（Select `class` 落点 / DataTable 列 scoped 类失效 / `showClear` 的 `hasValue` 口径）、**设计如此 2**（`optionValue` 非 `string \| number` 不渲染、Avatar 档位少于 PrimeVue，均已文档化）。**登记结果**：按 [规划规范 §3](../../standards/planning.md) 只登记 [Backlog](../../plan/backlog.md) §1.1（6 行，含 2 条条件候选）/ §1.6（迁移陷阱文档补强 1 行）/ §3（`null` 值支持不纳入 1 行），**不进入 `todo.md` 当前阶段**（Phase 14 未授权扩面）；下游均有可行写法，**不构成阻塞**；未向 momei 仓库提交 issue / PR，未改其文件。
- [2026-09-25-next-stage-scope-evaluation.md](./2026-09-25-next-stage-scope-evaluation.md)：下一阶段范围评估（Phase 13 归档后，**质量治理与组件增强取向**）。**触发**：用户指令「开始规划下一阶段待办」+ 取向裁定「取质量与治理收口 + 组件能力与长尾；下游迁移护航 / Phase 8 等待反馈暂不列入」。**结论**：可行且不依赖下游反馈即可独立闭环——a11y 是最「已就绪」的主线（装置与例外清单已入库、3 条例外 + 1 项行为观察均有真实 Chromium 取证），而取向 3 的真达标项偏少（T5 九项中 5 项条件触发 / 2 项已裁定不补 / 2 项口径冲突或长期），故 M5 以判定表收口、不强行扩面。**候选盘点**：T1~T7 合计 **49 项**（另 §7 具名 2 项），逐条有落点、无静默豁免（T6 为长期候选择要，不宣称即 Backlog 全集）。**主线草案**：M1 a11y 例外处置与受检面扩面（必选）/ M2 样式与 token 一致性收口（必选，含长期任务待执行批次「字段 shell 样式层共享」）/ M3 测试稳定性与质量装置（可选）/ M4 治理守卫精选（必选）/ M5 组件能力与长尾判定（可选）。**待决策 D1~D10**：主线取舍、a11y 例外处置取向、受检面扩面、字段 shell 共享形态、对比度处置、配对复核修复授权、flaky 深度、守卫取舍、组件能力取舍、是否发布新版本。**登记状态**：产出时为未分配阶段编号、未登记 `todo.md`。**决策结果（2026-09-25，见该记录 §10）**：D1 M1~M5 **全取**、D2 Toast 例外**维持现状**、D3 受检面**只纳入可稳定驱动者**、D4 字段 shell 取**语义 token 契约**、D5 对比度**仅盘点不改色**、D6 配对**仅出清单**、D7 flaky **定位并修**、D8 守卫**按容量补足**、D9 **只出判定表**、D10 **暂不发版**。**登记结果**：按 [规划规范 §4](../../standards/planning.md) 分配 **Phase 14（质量与一致性收口）**，同步 [todo.md](../../plan/todo.md)（5 条主线 / **14 条原子条目**；状态口径「登记 = 范围授权，实施未启动」）、[roadmap.md](../../plan/roadmap.md) 阶段表与状态行、[backlog.md](../../plan/backlog.md) 迁出标注、[recurring.md](../../plan/recurring.md) 字段 shell 形态裁定。
- [2026-09-24-phase13-m3-2-m3-3-release-and-freeze.md](./2026-09-24-phase13-m3-2-m3-3-release-and-freeze.md)：M3-2 / M3-3 迁移交付面收口（Phase 13）。**结论**：0.3.0 已发布——registry tarball shasum `b1a2f625…` 与发布日志逐字符一致（350 文件）、含 `dist/styles/index.css`（5,880 B）不含旧单体、四项子路径冒烟（根 89 导出含 `CaomeiTagsInput` / resolver / nuxt / theme.css）+ Vite 消费方构建（CSS 19.29 kB 含基础层与组件样式）通过、`npm view` = 0.3.0；版本 / CHANGELOG / annotated tag（`v0.3.0` → `92264fc`）一致，2 项偏差已登记（tag 视图不含 CHANGELOG 段 / 版本提交信息非 Conventional 形态）；发布后补跑 `pnpm verify` exit 0（首跑 2 例并发时序 flaky 已登记 Backlog 出现记录 ④）。**M3-3**：新增「0.x API 冻结窗口」节（中英版本策略页），声明冻结面（组件公开契约 / 子路径导出 / 公开导出名 / token 名称与语义）与非冻结面，对齐 dependfix §12 上收条件 3。**文档同步**：README（中英）版本、roadmap §1 版本句与 Phase 13 状态、Phase 13 条目计数 **10 → 11** 订正。**质量门**：`docs:check` 10 段全绿（parity 版本策略页 H2 4:4 / version 5 展示面派生自 0.3.0）/ `lint:md` / `docs:build` exit 0。**V 阶段显式跳过**（发布后校验 + 静态文档）。**Review Gate：R1 `Reject`（2 blocker / 3 warning / 3 suggest）→ 8 条 finding 修正 → R2 复审 `Pass`（0 blocker）**；R2 另 1 条非阻塞 suggest 已同批采纳（记为「已修复未复审」）。规模：6 文件 / +52 −6（唯一口径在该记录 §2）。**Phase 13 全部 11 条原子条目交付完毕**，阶段收口与归档待执行。
- [2026-09-24-m4-1-interpolation-guard.md](./2026-09-24-m4-1-interpolation-guard.md)：M4-1 文档站双花括号插值机检守卫交付与验证记录（Phase 13 M4 治理守卫精选，用户裁定 D9）。**结论**：新增 `pnpm docs:check:interpolation`（`scripts/docs/check-interpolation.mjs` + 26 tests），把 `docs/**/*.md` 中**围栏外**（含行内代码）的字面双花括号判为 `literal-interpolation` 并失败（消息带 `file:line` 与片段）——围栏代码块由 VitePress `v-pre` 豁免；允许插值页面登记在 `INTERPOLATION_ALLOWLIST`（中英《快速上手》与《版本与兼容策略》四页，仅放行消费 `theme.version` 的形态），登记表做**反向校验**（`allowlist-stale` 防清单腐烂）并与 `check-site-version` 的版本展示面机检同集合，对受检文件数下界与中英前缀覆盖设断言（`scan-scope-narrowed`）。**仓库级负向对照灵敏**（临时注入围栏外 / 行内 / 围栏内三形态 → 前两行精确命中、围栏内不报，exit 1；还原后 exit 0），**全库零误报**（受检 211 md）。**门禁接线**：`docs:check` 由 9 段扩为 **10 段**，随 `governance:check` 进入 `verify` 与 CI。**质量门**：lint:check / typecheck / test（88 文件 / 1793 tests）/ `docs:check` 10 段全绿 / `governance:check` / `docs:build` exit 0。**V 阶段显式跳过**（Node 守卫 + 静态文档，无 UI 面）。**Review Gate：R1（`standard`）`Pass`（0 blocker / 2 warning / 4 suggest，实测约 5 分钟）→ 6 条 finding 修正 → R2 复审 `Reject`（1 blocker：§2 计数为修复前快照、不可复算）→ 计数刷新 → R3 复审 `Pass`（0 blocker）**。规模：4 文件 / +540 −1（唯一口径在该记录 §2）。**残留**：M3-2（0.3.0，依赖 npm 凭据）/ M3-3 未启动。
- [2026-09-23-m3-migration-delivery.md](./2026-09-23-m3-migration-delivery.md)：M3 迁移交付面交付与验证记录（M3-1 迁移映射与不支持清单收口；Phase 13）。**结论**：只补**指南层**（逐组件映射权威仍是[设计规范 §7](../design-spec.md)，M1 / M2 已登记 `DataTable` 三项能力与 `TagsInput`，本条目不改 §7）——[从 PrimeVue 迁移](../../guide/primevue-migration.md)（中英）「常见陷阱」表新增 3 行（**标签录入**：`Chips` / `InputChips` → `CaomeiTagsInput` 首选、`AutoComplete + multiple` 备选；**表格排序模型**：`sort-mode` / `multi-sort-meta` / `default-sort-order` → `sortMode` / `multiSortMeta` / `sortDescFirst`；**表格分组与行展开**：同名 camelCase + `v-model:expandedRowGroups` / `v-model:expandedRows` + `#groupheader` / `#expansion`），并在「逐组件对照入口」表单输入行补 `TagsInput`；3 行均以「见 §7 与 <组件页>」收口，不构成第二事实源。**回扫**（`test-results/m3-1/verify-migration-index.mjs` exit 0）：入口表组件 **zh 47 / en 47** 与 §11 登记表 **47** 集合一致、47 个组件名均在 §7；中英各 47 个组件页均有迁移节；「不宣称穷尽」表述保留。**质量门**：`docs:check` 9 段链全绿（structure 210 页 + 侧栏 47 条目 / parity 59 对 / showcase 13 项）、`lint:md:check` 通过、`docs:build` **exit 0**（无 dead link / 无渲染 TypeError）。**V 阶段显式跳过**（纯 markdown 静态表格行，无组件 / 样式 / 交互面）。**Review Gate 第 1 轮（`standard`）`Pass`**（0 blocker / 1 warning / 3 suggest；实测约 6 分钟）——RG-W01（审计请求预写「已回写治理记录」的不实陈述）由本记录闭合；RG-S01（陷阱表 ↔ §7 无自动对账）保留（§15 已列「指南表」为通用载体）；RG-S02（补 `v-model:` 前缀）/ RG-S03（回写 M2 记录残留项闭合）已同批修正。规模：2 文件 / +4 −1（唯一口径在该记录 §2），单批提交。残留：**M3-2**（0.3.0 版本交付，依赖 npm 凭据）与 **M3-3**（0.x 冻结窗口声明）未启动。

- [2026-09-23-m2-tags-input.md](./2026-09-23-m2-tags-input.md)：M2 `TagsInput` 组件交付与验证记录（M2-1 实现与接线 / M2-2 组件页与示例 / M2-3 登记面接入；Phase 13）。**结论**：新增 `CaomeiTagsInput`（封装 Reka `TagsInput` primitive，用户裁定 D5）——多值 `v-model`（`string[]`）、回车 / 分隔符提交、粘贴按分隔符拆分（`addOnPaste` **默认 `true`**）、点击删除 + Backspace 两段式键盘删除、`max` / `allowDuplicate`（**默认 `false`，与 PrimeVue 默认相反**）/ `invalid` / `disabled` / `size` / `showClear` / `label` 等字段契约，事件 `addTag` / `removeTag` / `invalidInput`；按既有交付面齐备（类型 / 导出 / Nuxt 注册 / locale `tagsInput.clear` 5 语种 / a11y 夹具 47→48 / 中英组件页 8 个 H2 对称 / 中英示例各 3 个 / 设计规范 §7）。**包装层 a11y 修复**：标签项显式 `role="group"`（Reka 在 `role=generic` 上输出 `aria-labelledby`，ARIA 1.2 禁止在 generic 命名）→ axe `V=0 / I=0`，**未新增例外清单条目**；有标签时字段根输出 `data-filled` 接入 FloatLabel。**迁移口径**：`Chips`（v4 起为 `InputChips` 旧名）迁移**首选本组件**，`AutoComplete + multiple` 降为**备选**（组件页 + §7 三处一致）。**V 阶段（`@ui-validator`，真实 Chromium）15 项全通过**：真实按键序列的回车 / 分隔符提交、粘贴拆分、点击删除与 Backspace 两段式、去重与超限拒绝、清空后 `data-filled` 移除 + 焦点回输入框、档位几何（sm 29.19 / md 36 / lg 44px）与 focus 环 `color-mix`、FloatLabel 上浮不重叠与回落、表单隐藏控件 `name[tags[0]]`、四档溢出 0、中英噪声 0。**质量门**：`pnpm test` 87 文件 / 1767 tests、`test:a11y` 55、`check:locale-keys` 26 命名空间 / 76 条 × 5、`docs:check` 9 段链全绿（parity 59 对）、`capture:styles` 239 项 0 差异、`pnpm verify` exit 0。**Review Gate**：M2-1 **R1 `Reject`（1 blocker：UI 组件缺浏览器证据，7 分 34 秒）→ 建立验证载体并完成真机验证 → R2 `Pass`（0 blocker，8 分 12 秒）**，R2 另更正 V 证据中 sm 高度失准值（34 → 29.19px）；M2-2 **R1 `Pass`（0 blocker / 2 warning / 3 suggest，9 分 05 秒）**，W1/W2/S1/S2/S3 已同批修正（记为「已修复未复审」）。规模：M2-1 面 17 文件 / +719 −6、M2-2 面 9 文件 / +414 −0（唯一口径在该记录 §7），按交付面拆两次提交。**M2-3 登记面接入已交付**（记录 §11~§13）：§11 登记表 / 中英侧栏 / 中英总览页 / 画廊登记表（13 项）+ 组件清单 §5，共 6 文件 / +17 −2；`docs:check` 9 段链全绿（侧栏 6 组 / **47** 条目、画廊 **13** 项），构建产物 SSG 静态断言 18/18（TagsInput SSR 真实渲染），Review Gate R1 **`Pass`**（0 blocker / 1 warning / 3 suggest；W1 / S1 已同批修正、S2 留 M3-1、S3 维持 Backlog 在册）。**M2 三条原子条目全部交付**。
- [2026-09-23-m1-4-multi-sort.md](./2026-09-23-m1-4-multi-sort.md)：M1-4 `DataTable` 多列排序与降序优先交付与验证记录（Phase 13，**M1 收口条目**）。**结论**：按 PrimeVue 命名补多列排序——`sortMode="multiple"` + `v-model:multiSortMeta`（`DataTableSortMeta[]` = `{ field: 列 key, order: 1 \| 0 \| -1 }`，按优先级排列）+ `update:multiSortMeta`；**降序优先** `sortDescFirst`（表格级布尔量，决定首次点击方向，单列与多列均生效）。**交互（对齐 PrimeVue 的 `addMultiSortField` 语义）**：点击表头按住 Cmd / Ctrl 追加为下一个排序键（已在列表中则翻转方向、不影响其它键），不按修饰键收敛为该列单列排序；已排序表头渲染**优先级序号**；排序键按「升 → 降 → 移除」循环。**有意差异（4 条）**：`field` 只接受列 key（上游允许字段函数）、`order` 只接受 `1 \| 0 \| -1`（上游允许 `undefined` / `null`）；上游 `defaultSortOrder` 有表格级与列级两种形态、本库只提供表格级；上游 `removableSort` 开关未提供（固定循环）；多列模式下 `sort` 载荷额外带 `multiSortMeta`。**V 阶段（`@ui-validator`，真实 Chromium）9 项全通过**：初始受控（行序 `李四/张三/赵六/王五`、序号 `[1,null,2]`、chevron `[up,无,down]`、`aria-sort=["ascending","none","descending"]`）/ Ctrl 追加后序号 `[1,3,2]` 与翻转 / 无修饰键收敛为 `[null,1,null]` / 三击移除后序号全空且行序回原 / 亮暗序号与 chevron 实测色值 / 四档溢出 0 / 中英两页四类噪声 0 / 四个既有示例无回归。**质量门**：`pnpm test` 86 文件 / 1737 tests、`docs:check` 9 段链全绿、`check:design` / `test:a11y` 通过、`capture:styles` **239 项 0 差异**（单列路径零漂移硬门禁）。**Review Gate 第 1 轮（`standard`）：`Pass`（0 blocker / 1 warning / 3 suggest；实测 6 分 02 秒）**——RG-W01（设计规范 §7 保留「内部固定 `sortDescFirst: false`」等旧表述、与同段新增句矛盾）已改写；RG-S02（`multiSortMeta` 浅响应未声明）已补；RG-S03（单列不抛 `update:multiSortMeta`、多列三击移除）已各补 1 条断言；RG-S01（序号并入可访问名）保留并已在无障碍节声明。审计方以 tanstack 源码实锤「缺省 `sortDescFirst` 的零漂移成立」与「VTU 修饰符语法可靠」。规模见该记录 §6（唯一口径）。按交付面拆两次提交。
- [2026-09-23-m1-3-row-expansion.md](./2026-09-23-m1-3-row-expansion.md)：M1-3 `DataTable` 行展开（expander 列 + `#expansion`）交付与验证记录（Phase 13）。**结论**：按 PrimeVue 命名补行展开——`columns` 项 `{ key, expander: true }` + `v-model:expandedRows`（`string[]` 行 key，受控优先 / 缺省自持）+ `#expansion="{ data, index }"` + `@row-expand` / `@row-collapse`（载荷 `{ originalEvent, data }`）；展开列单元格渲染内建原生 `<button>`（chevron + `aria-expanded` + 条件化 `aria-controls` + 可访问名），表头**始终留空**（模板层单点守卫；多个 `expander` 列仅首个生效）；展开行跨全部数据列、不参与冻结列吸边；未提供 `#expansion` 槽时无可见展开区（与上游一致）。**有意差异（6 条）**：`expandedRows` 仅支持行 key 数组；展开按钮可访问名**按动作取名**（上游 `expandButtonAriaLabel` 与动作相反，一方源码核对）；展开行不吸边；`#expansion.index` 为显示序号；无槽无可见区；`aria-controls` 仅在展开行实际渲染时输出（上游始终输出、收起态悬空）。**V 阶段（`@ui-validator`，真实 Chromium）两轮全通过**：第一轮 10 项（初始受控展开 1 行 / 往返与提示同步 / 嵌套 `<ul>`、`<li>` / 三按钮 `aria-controls` ↔ 展开行 id 全等 / Tab + Enter·Space 且四步焦点保留 / `th=3` 与 `td=[3,3,3]`、`colspan=3` / 四档溢出 0/0 / 亮暗 / 中英噪声 0 / 无回归）；修复点落地后第二轮聚焦复验（收起态 `aria-controls` 属性存在性 `[true,false,false]`、展开后逐项匹配 `["v-6-expansion-0","v-6-expansion-1"]`、展开列 `th` 无排序按钮）同样全通过。**质量门**：`pnpm test` 85 文件 / 1718 tests、`check:locale-keys` 5 语种各 75 条、`docs:check` 9 段链全绿、`check:design` / `test:a11y` 通过、`capture:styles` **239 项 0 差异**。**Review Gate 第 1 轮（`standard`）：`Pass`（0 blocker / 1 warning / 5 suggest；实测 7 分 21 秒）**——W1（「表头留空」仅在默认表头路径生效，`expander + sortable` 会漏出排序按钮）改为模板层单点守卫并补断言；S1（文档对 `rowKey` 缺省错位条件的表述过宽）按 `row.index` 语义更正为「整体替换 `data`」；S2（收起态 `aria-controls` 悬空）改为条件输出并刷新 V 证据；S4（提交切分）采纳；S3 / S5 未采纳并登记理由。审计方明确接受超阈值理由（18 文件 / +737 −50 时判定）。规模与超阈值说明见该记录 §6（唯一口径）。按交付面拆三次提交。
- [2026-09-23-m1-2-expandable-row-groups.md](./2026-09-23-m1-2-expandable-row-groups.md)：M1-2 `DataTable` 可折叠分组（expandableRowGroups）交付与验证记录（Phase 13）。**结论**：在 M1-1 行分组之上按 PrimeVue 命名补可折叠分组——`expandableRowGroups` + `v-model:expandedRowGroups`（`string[]` 分组键，受控优先 / 缺省自持）+ `@rowgroup-expand` / `@rowgroup-collapse`；**缺省时分组全部收起**（对齐 PrimeVue），收起分组的数据行不进入渲染条目、不影响分页器总条数；分组键取字段值的字符串形式，非连续同值分组共用同一键。**有意差异**：① 切换按钮补 `aria-expanded` 与随状态切换的 `aria-label`（PrimeVue 仅 chevron、两者皆无），并在无障碍段声明取名取舍；② 事件 `data` 为归一化字符串键（上游传原值）；③ 承 M1-1 的冻结列吸边与空白占位两项差异。**V 阶段（`@ui-validator`，真实 Chromium）9 项全通过**：缺省 2 组 0 数据行且两 toggle `aria-expanded=false` / 展开往返与 chevron + 提示同步 / 多组独立 / Tab 可达 + Enter·Space 切换且 4 步焦点保留 / 展开后 `th=2` 与 `td=2` 逐列 x 区间 delta 全 0 / 四档溢出 0 / 亮暗实测 / 中英两页四类噪声全 0 / 不可折叠示例无回归。**质量门**：`pnpm test` 84 文件 / 1703 tests、`check:locale-keys` 5 语种各 73 条、`docs:check` 9 段链全绿（integrity 243 md / links 242 md / structure 204 页）、`check:design` / `test:a11y` 通过、`capture:styles` **239 项 0 差异**。**Review Gate 第 1 轮（`standard`）：`Reject`（1 blocker）**——内置文案台账 `docs/components/locale.md`（中英）未同步两个新键且无门禁覆盖；实测用时 6 分 46 秒（未超 10 分钟时间盒）。B1 与 6 条 suggest 已同批修正（台账补键 + 台账对账守卫登记 Backlog、非连续同值用例、模板条件单点化、`aria-label` 取舍声明、跨类型同键边界、watch `deep`、斑马纹说明）；**第 2 轮（只审修复点）`Pass`（0 blocker，实测 5 分 43 秒）**，转 follow-up 的 F1（索引同句计数矛盾，复发）/ F2（规模口径未钉持久 ref）/ F3（`deep` 缺直接断言）已同批修正，F4（台账对账守卫）按 Backlog 排期，F5（auto-complete 偶发失败）已追加出现记录。审计方明确接受超阈值理由（22 文件但行数未超 800，6 个为 2 行级载体且受 5 语种对等 / 中英 parity 门禁约束不可拆分）。规模 22 文件 / +664 −16，按交付面拆三次提交。
- [2026-09-23-m1-1-row-grouping.md](./2026-09-23-m1-1-row-grouping.md)：M1-1 `DataTable` 行分组（subheader）交付与验证记录（Phase 13）。**结论**：按用户裁定 D2 以 PrimeVue 命名补齐行分组——`rowGroupMode="subheader"` + `groupRowsBy`（支持点号嵌套路径）+ `#groupheader="{ data, index, groupValue }"`（未提供插槽时回退渲染分组键取值）；按**连续同值**切分、只在当前渲染行序（排序 + 分页切片）上计算，分组标题行跨全部数据列且不参与冻结列吸边。**有意差异（逐条）**：① 分组列在数据行渲染**空白占位单元格**而非像 PrimeVue 那样不渲染（后者使数据行左移一列、与表头错位，primefaces/primevue#6496），复验实测表头与数据行逐列 x 区间 0 偏差；② `#groupfooter` 与 `rowGroupMode="rowspan"` 不实现。**V 阶段（`@ui-validator`）两遍**：第一遍暴露 O1 列错位 → 当轮修复 → 复验 8 项全通过（列对齐 0 偏差 / 每行 td 数 = th 数 = 3 / 分组标题 colspan=3 且 `role="row"` / 分组键 `aria-pressed` 双向切换 / 四档横向溢出 0 / 亮暗 token 实测 / 中英两页 console · pageerror · HTTP≥400 · requestfailed 全 0）。**质量门**：`pnpm test` 84 文件 / 1691 tests、`docs:check` 9 段链全绿、`check:design` / `test:a11y` 通过、`capture:styles` **239 项 0 差异**（默认关闭零漂移硬门禁）。**观察项（接受）**：亮色下分组行背景与数据行仅差 1/255，区分靠字重与边框，且本阶段不改 token 色值。**Review Gate 第 1 轮（`standard`）：`Pass`**（0 blocker / 3 warning / 4 suggest；实测用时 4 分 12 秒，未超 10 分钟时间盒），7 条修复点（JSDoc 与实现相反、记录计数与口径、暂存区卫生、零漂移断言、`striped` 相位说明、`accessor` 口径、边界用例）已同批修正，记为「已修复未复审」。规模 12 文件 / +768 −42，拆两次提交。结论见该记录 §8。
- [2026-09-23-next-stage-scope-evaluation.md](./2026-09-23-next-stage-scope-evaluation.md)：下一阶段范围评估（Phase 12 归档后，**dependfix 迁移优先**）。**触发**：用户指令「开始规划下一阶段待办；为了不阻塞 dependfix 发版，下阶段可以考虑先补全缺失的组件」。**结论**：可行且关键路径明确——下游 dependfix `apps/platform` 迁移评估已判定「可行（有条件）」，23 个 PrimeVue 组件中 21 个有等价组件，缺口集中在 **`DataTable` 行分组 / 行展开 / 多列排序**（本仓源码 0 命中）+ `TagsInput`（无对应组件）+ 若干页面侧改写。**候选盘点**：T1~T6 共 **49 项**，逐条有落点、无静默豁免（T5 的 31 项长期候选留 Backlog）。**主线草案**：M1 `DataTable` 能力补齐（关键路径）/ M2 `TagsInput` / M3 迁移交付面（文档映射 + 0.3.0 + 0.x 冻结窗口）/ M4 治理守卫（可选）。**待决策 D1~D9**：主线取舍、`DataTable` 新能力的 API 命名取向（最关键）、受控模型、降序优先、`TagsInput` 形态、`Sidebar` 是否补、是否发布 0.3.0、Phase 8 是否随接入启动、M4 取舍。**决策结果（2026-09-23，见该记录 §11）**：D1 取 **M1+M2+M3+M4**（全取）、D2 **对齐 PrimeVue 命名**、D3 受控与自持两者都支持、D4 提供 `sortDescFirst`、D5 封装 Reka primitive、D6 不补 `Sidebar`、D7 发布 **0.3.0**、D8 Phase 8 维持未启动、D9 纳入 1 项守卫。**登记结果**：按[规划规范 §4](../../standards/planning.md) 分配 **Phase 13（组件能力补齐与 dependfix 迁移解阻）**，同步 [todo.md](../../plan/todo.md)（4 条主线 / 11 条原子条目；状态口径「登记 = 范围授权，实施未启动」）、[roadmap.md](../../plan/roadmap.md) 阶段表与状态行、[backlog.md](../../plan/backlog.md) 4 行迁出标注。
- [2026-09-22-m2-5-component-gallery.md](./2026-09-22-m2-5-component-gallery.md)：M2-5 组件画廊交付与验证记录（Phase 12 条件条目）。**结论**：新增中英「组件画廊」页（`/components/showcase` + `/en-US/components/showcase`），以登记表 `docs/.vitepress/showcase-registry.json`（12 项 / 覆盖 §11 全部 6 分组）驱动真实组件预览卡片；入口走中英总览页与指南侧栏，**不进组件侧栏**（§11 侧栏不变式为机器校验面）。**守卫**：新增 `docs:check:showcase`（`scripts/docs/check-showcase-registry.mjs`，20 tests），对账结构 / `example` 形态 / 分组归属 / 中英组件页与示例存在性 / 登记顺序 / 挂载点，并设项数与分组数下界；仓库级负向对照两次（未知分组、示例目录错配 → exit 1，还原后 exit 0）。**V 阶段（@ui-validator）7 项全通过**：12/12 卡片真实渲染、四档 1440 / 1024 / 768 / 375 溢出 0px（列数 2/2/2/1）、亮暗一致、中英同步、5 类交互可用、12×2 链接 200 且 locale 正确、console / pageerror 0。**Review Gate 两分区并行 R1 双 Pass（0 blocker）**；修复点（`withBase` 链接、挂载点边界正则、描述与文档措辞）已同批修正并复验（含 `VITEPRESS_BASE=/caomei-ui/` 实机构建），记为「已修复未复审」。规模：13 文件 / 985 行新增，拆两次提交。
- [2026-09-22-m2-1-nav-overflow-fix.md](./2026-09-22-m2-1-nav-overflow-fix.md)：文档站导航栏 768–959px 横向溢出修复（Phase 12 M2-1 后续，用户裁定方案 b）。**结论**：`/en-US/**` 在 768px 的溢出由 **146px → 0**（既有 79px + M2-1 版本项 67px），1440 / 1024 / 375 与 959/960 断点无回归，导航 `v0.2.0` 条目与主题预设 `<select>` 两处展示均保留可用。**手段**：仅窄档（`max-width: 959px`）收敛——隐藏切换器标签 + 左边距 12→4px（≈56px）、导航项 `padding: 0 5px; font-size: 13px`（≈90px）；≥960px 保持默认主题原样。**复验（@ui-validator）**：目标页 diff=0、三档 27 组 diff=0、切换器持久化与预设配色生效、暗色 diff=0、有效页 console/pageerror 0。**过程发现**：英文规范子页与 `/en-US/guide/` 无 index 属 §10 设计口径（非缺陷）。
- [2026-09-22-phase12-m1-release-execution.md](./2026-09-22-phase12-m1-release-execution.md)：Phase 12 M1 手动发布执行与发布后校验（0.2.0）。**结论**：`caomei-ui@0.2.0` 已手动发布（`latest` = 0.2.0，registry 发布时间 2026-09-22T12:21:29Z；tarball 345 文件 / unpacked 779,993 B / shasum `d29c75bd…`）。**校验**：从 registry 下载的 tarball 含 `dist/styles/index.css`、**不含**旧单体 `dist/styles.css`；干净目录安装后四项子路径冒烟通过（根 88 导出 / resolver / nuxt（需可选 peer `@nuxt/kit`）/ theme.css 5,880 B 含 token 与 `.caomei-root`）；Vite 消费方构建冒烟通过（产物 CSS 含基础层 + 组件样式）。**已知限制**：裸 Node ESM 不能直接导入产物（`css.inject` 保留 CSS import），已在 CHANGELOG 破坏性段与发布指南 §9 声明。**偏差登记**：tag `v0.2.0` 指向版本提交、CHANGELOG 提交在其后（tag 视图不含该段，按指南不重写已发布 tag）；CHANGELOG 存在空 `# Unreleased` 段（生成器健壮性候选入 Backlog）。载体同步：README（中英）/ 快速上手（中英）/ 发布指南 §4·§9（含下游修复指引，英文侧新增等价小节）/ 路线图 §1 / 待办事项。
- [2026-09-22-docs-versioning-reevaluation.md](./2026-09-22-docs-versioning-reevaluation.md)：文档站版本化形态再评估（Phase 12 M2-1）。**结论（用户裁定 D1-B）**：原 M2-1「多版本托管」在 0.x 开发期收益为 0 而成本持续，且受**架构性约束**——文档站与工作区源码强绑定（demo 经 `@ → src/` 别名编译、API 表由 `src/` 现场生成、站点直引源码样式），同一构建内托管旧版文档会让其 demo / API 绑到当前源码 → 不可信；社区插件式方案恰要求旧版文档自包含，与「文档与源码同源演进」冲突。**多源核对（2026-09-22）**：VitePress 无内置版本化；`@viteplus/versions`（2.0.7 / 6 stars / 2025-09 建库）为冻结副本式，`vitepress-versions` 自述 alpha 不适合生产，`mvb` 绑主题；主流 Vue 项目（Vue Router / Pinia / VueUse）走独立部署而非单站插件。**处置**：M2-1 改写为轻量形态「版本信息与兼容策略」（当前版本 + 获取渠道 + 0.x 破坏性变更策略与下游 pin 建议 + 版本号单点机检）；多版本托管退回 Backlog 并附触发条件（同时维护 ≥2 个对外版本，或下游按版本 pin 并要求旧版文档）。
- [2026-09-22-m4-2-a11y-gate-wiring.md](./2026-09-22-m4-2-a11y-gate-wiring.md)：M4-2 a11y 断言接入门禁（Phase 12）。**结论**：把 M4-1 的既有例外清单从记录搬进机检数据（`test/a11y/exceptions.ts`，含判定依据指针）并落**两向断言**——① 每个组件的 `violations` / `incomplete` 必须 ⊆ 已登记例外（**例外外零违规**）；② 已登记的例外**必须仍然命中**（防清单腐烂：修复落地后须重新裁定并删除该条）。**门禁路径**：断言位于 `pnpm test` → 随 `pnpm verify` 与 CI 合并门禁（`test.yml` 的 `test-build` job 跑 `pnpm test:coverage`）生效，定向入口 `pnpm test:a11y`；**门禁强度相对现状只增不减**（新增会失败的断言，未放宽既有检查、未把浏览器依赖引入 `verify`）。**负向对照两向实测灵敏**：删掉 Toast 例外 → 报「未登记的 violation」；加一条不命中的例外 → 报「未再命中」。**连续零失败**：`pnpm test:a11y` 连续 5 次 54 passed；全量 `pnpm test` 连续 2 次 1506 passed（并行负载无新增 flaky）。**边界**：例外判定证据仍以 M4-1 记录 §3 为准（本清单只保留结论 + 指针，避免双权威）；`incomplete` 与 `violation` 分开断言；axe 版本敏感，升级后须重跑并重新裁定。
- [2026-09-22-m4-1-a11y-baseline-inventory.md](./2026-09-22-m4-1-a11y-baseline-inventory.md)：M4-1 可访问性既有例外清单建立（Phase 12）。**结论**：以 `axe-core`（devDependency，版本以 lockfile 为准）× Vitest `happy-dom` 建立组件级审计装置（`test/a11y/`：受检面声明 / 规则面 / 审计器 / 测试；命令 `pnpm test:a11y`，53 tests / 6 秒级）。**受检面口径可复验**：受检单位为组件族根组件（47 夹具），对外导出（`src/nuxt/components.ts` 的 `caomeiComponents`，79 个）按四组穷尽登记并由单测机检——47 夹具根 + 19 个由夹具组合渲染（运行期断言）+ 12 个需交互展开面板（不在本轮受检面，逐条理由 + 触发点）+ 1 个不渲染自有 DOM（`CaomeiConfigProvider`）。**规则面**：16 条禁用规则逐条带理由（6 条页面级结构 + 10 条依赖布局引擎，后者依 axe 的 JSDOM 支持说明与 axe-core#4021 逐条自证），其余默认规则全部生效。**既有例外清单 3 条（全部已裁定，无静默豁免）**：① `CaomeiToastProvider` 的 `aria-hidden-focus` ×2（Reka `Toast/FocusProxy` 的 `VisuallyHidden tabindex="0"` 焦点哨兵，上游有意模式）；② `CaomeiMultiSelect` 的 `aria-valid-attr-value`（关闭态 `aria-controls=""`，真实 Chromium 实测开启态指向正确面板 id）；③ `CaomeiCalendar` 的 `aria-prohibited-attr`（`aria-label` 落在 `role=generic`，真实 Chromium AX 树实测名称可解析为 `日历, 2026年9月`，规范上仍属偏差）。**边界**：受检状态为默认（关闭）态（Dialog/Drawer 以 open 挂载），12 个面板内导出与依赖布局的规则（对比度 / 触摸目标 / 焦点顺序）不在受检面并已登记触发点；`passes` 计数不等于覆盖充分；门禁断言（例外外零违规）留 M4-2。另发现「无 `CaomeiStepperDescription` 的步骤产生悬空 `aria-describedby`」，与 3 条例外修复候选一并入 [Backlog](../../plan/backlog.md)。
- [2026-09-22-m3-5-computed-style-capture-landing.md](./2026-09-22-m3-5-computed-style-capture-landing.md)：M3-5 计算样式取证装置入库（Phase 12）。**结论**：M2-2 / M3-1 / M3-3 / M3-4 的一次性采集装置（`.temp/capture/`，gitignored）迁入 `test/capture/`——聚焦夹具（独立 Vite 应用，消费 `src/` 源码）+ 采集运行器 `capture.mjs` + 比对运行器 `diff.mjs` + **冻结基线** `baseline.json`（`--freeze` 生成，随装置同提交），命令 `pnpm capture:styles` / `pnpm capture:styles:freeze`，接入周级回归 `regression-weekly.yml`；运行时按声明自检受检面（缺失 / 选择器未命中即失败）。**采样面 239 项**（size 21 / tier 26 / state 8 / trigger 12 / variant 50 / button 54 / button-focus 54 / 局部层叠 6 / 浮层 8）；相对一次性装置 262 项**未纳入 23 项**（浮层面板 z-index 9 / toast 视口与 tone 6 / confirm-dialog 2 / 小屏媒体查询档位 6，逐条登记依据与触发点）。**等价证据**：一次性装置在 HEAD 复跑与其历史产物 **262 项 0 差异**（同源确认）→ 与入库装置按前缀映射对照 **交集 239 项 0 差异**、仅存于旧装置 23 项（与登记一致）、仅存于新装置 0 项；**负向对照**（md 档 input 高度 `+1px`）报 1 处差异、还原后 0 差异。**迁移期捕获两处真实缺陷**：聚焦采样目标写错（`:focus-within` 未触发）、Reka 实例计数器（`-v-<n>`）随夹具漂移未归一。**已知边界**：基线绑定单一视口与 Chromium 版本；不替代常驻 E2E 的浮层几何断言；`pnpm verify` 不含该装置（需浏览器，与 `check:coverage` 同类落位）。
- [2026-09-21-m3-4-trigger-unstyled-convergence.md](./2026-09-21-m3-4-trigger-unstyled-convergence.md)：M3-4 触发器 `unstyled` 遗留收敛（Phase 12）。**结论**：date-picker / color-picker / split-button 三处「直连 Reka primitive 触发器 + `as-child`」收敛为本库 `CaomeiPopoverTrigger` / `CaomeiDropdownMenuTrigger`（`as-child` + `unstyled`），触发器行为变更可单点生效、库内改用自己已文档化的公开用法。**明确边界**：本收敛**不新增 a11y 能力**（本库触发器是 Reka 的薄包装）。**等价证据**：真实 Chromium 触发结构 A/B **262 项逐属性 0 差异**（属性快照 + 7 项计算样式，含闭合 / 开合两态）；**负向对照**去掉 `unstyled` → 5 处差异（内建类合并 + `color` / `font-size` 漂移），证明探针灵敏且 `unstyled` 生效。**质量门**：`pnpm verify` exit 0（1435 tests）/ `pnpm test:e2e` exit 0（54 passed）/ 定向 3 组件 85 tests。**坑与缺口**：开合态采样须置于 toast 采样之后（否则瞬时 toast 超时丢 6 项）；属性快照须过滤 `data-v-*`；采集装置在 `.temp/`（gitignored）待 M3-5 入库。
- [2026-09-21-m3-1-size-tier-normalization.md](./2026-09-21-m3-1-size-tier-normalization.md)：M3-1 尺寸档位 `:where()` 归一化与计算样式等价验证（Phase 12）。**结论**：8 组件 28 条非 `:where()` 尺寸档位块全部归零（基类改 `var(--caomei-<comp>-<prop>, 默认回退)` 消费、档位块改 `:where(...)` 只声明变量）；`rg` 归零核验 0 命中。**等价证据**：真实 Chromium 计算样式矩阵 **242 项逐属性 0 差异**——新增 `m31` 段 26 项覆盖 6 组件（input / textarea 含 `__control` / input-number 含 `__button` / date-picker / tag / badge，badge 另含 `dot` 的 md / lg），**button 与 select 的档位几何由既有矩阵覆盖**（`button.*` + `button-focus.*` 含 height / padding-left / font-size；`size.select.*` 含 height / padding / font-size）；既有 216 项同批零漂移。**唯一复合块处理**：`.caomei-badge--dot.caomei-badge--lg` → `.caomei-badge--dot:where(.caomei-badge--lg)`（`--dot` 保留常规特异性以免被 8px 覆盖，实测 `dot + lg` 仍 10px）。**质量门**：`pnpm verify` exit 0（1415 tests）/ `pnpm test:e2e` exit 0（54 passed）/ lint:css / check:design / typecheck 通过。**缺口**：采集装置在 `.temp/`（gitignored）不可从仓库复算 → 由 M3-5（自 M3-2 拆出）入库。
- [2026-09-21-next-stage-scope-evaluation.md](./2026-09-21-next-stage-scope-evaluation.md)：下一阶段范围评估（Phase 11 归档后）。**产出时点状态**：无进行中阶段；Backlog 在册 **46 条**（本次合并 2 组重复候选，并修复 `roadmap.md` 两处陈旧「进行中」标记）；承接状态见 §2 表 7 行，归为 6 类（已闭环 / Phase 11 §6 明确延后 / Phase 11 新增未裁定遗留 ①、②（同属一类）/ 载体一致性缺口 / 口径分叉 / 外部依赖），并新识别一处**口径分叉**——`main` 已是 `unbundle + css.inject` + `./theme.css` 形态，而 npm 0.1.0 仍为旧单体 `styles.css` 且 `version` 未递增（破坏性形态变更未随版本号表达）。含 T1~T6 六组候选（发布链路 / 工程可信度 / 文档站门面 / 样式一致性 / 组件长尾 / 治理守卫；T1~T4 与 T6 标注来源 / 规模 / 风险 / 依赖，T5 为条件触发表）与三个阶段组合建议——**P「发布就绪与对外门面」（建议优先）** / Q「工程可信度补全」 / R「一致性收官与治理守卫」。**待决策 D1~D8（产出时点快照；已全部裁定，见下）**（取向 / 主线 / 启动授权 / CI 发布凭据方式 / 版本发布 / 28 条 `:where()` / 文档站观感 / Phase 8）。**登记结果（2026-09-21）**：用户 D1~D8 全部裁定并**授权启动**——取向 P / Q / R 混合、不启用 CI 自动发布（手动发布）、发布 0.2.0、28 条 `:where()` 收敛、观感列为条件条目、Phase 8 维持未启动（等待下游完成接入）；按 [规划规范 §4](../../standards/planning.md) 分配 **Phase 12（发布就绪、文档对外与一致性收官）**，同步 [todo](../../plan/todo.md)（5 条主线 / 20 条条目）/ [roadmap](../../plan/roadmap.md) / [backlog](../../plan/backlog.md)（15 行迁出标注），见该记录 §6 决策结果与 §9 登记结果。
- [2026-09-20-m2-2-m2-3-style-governance-landing.md](./2026-09-20-m2-2-m2-3-style-governance-landing.md)：M2-2 / M2-3 样式治理落地（Phase 11）。**结论**：按用户 2026-09-20 裁定「全面收敛」落地——**G2** `--caomei-<comp>-*` 声明 `:where()` 归一 + 基类 fallback（9 组件）、**G1** 档位块只声明 CSS 变量（5 组件）、**G4** 新增 9 个 `--caomei-z-*` 并收敛 21 处、**D1** 删 4 处死声明；`check:design` 新增 G1~G4 机检（27 单测）并修复 `scanRules` 语句型 at-rule 缺口。**等价证据**：计算样式矩阵 **226 项 0 差异**（既有 216 + 本轮 toast viewport 6 / drawer 四向 4 的几何采样；基线取 `HEAD` worktree，含 421px 负向对照命中 8 处）。**质量门**：`pnpm verify` exit 0（73 文件 1403 tests）/ `pnpm test:e2e` exit 0（54 passed）。**残余风险（W2）**：28 条（27 单一 + 1 复合）非 `:where()` 尺寸档位块（8 组件）不在 G1/G2 拦截面内且当前生效（规范偏差，非可见缺陷），**未收敛待用户裁定**；结构型 `:where()` 与预留 token 取舍见记录 §6。
- [2026-09-20-m2-1-component-quality-audit.md](./2026-09-20-m2-1-component-quality-audit.md)：M2-1 重量级组件质量盘点（Phase 11）。**结论**：7 组件在 momei **均有真实用量**（PrimeVue 写法 2 / 3 / 8 / 1 / 1 / 1 / 2，Caomei 写法 0——迁移未开工）；候选门槛判定——**ColorPicker 色板导航不达标**（`swatches` 0 命中）留 Backlog，**AutoComplete 严格选项模式部分达标待用户裁定**（受控字段 `translationId` 1 处，需新增 props，建议暂不纳入）。盘点发现 **D1 死声明 4 处**（同属性重复声明被覆盖）与 **D2~D4 scoped 变量/档位声明偏差**（drawer 基类预声明 + 档位类非 `:where()`；auto-complete / multi-select 档位块直接声明几何属性）、**Z1 `z-index` 字面量 5 处**；已达标项：无原始色值、无 `opacity` 字面量、无 `!important`。经 `@code-reviewer` Review Gate 四轮（R1~R3 Reject → R4 Pass，修正在于取证计数口径 / 行号笔误 / 规则面收束）。实施清单见记录 §4（移交 M2-2 / M2-3）。**用户裁定（2026-09-20）**：M2-2 / M2-3 选**全面收敛**（G1 含 select / select-button；G2 含 dialog / confirm-dialog / radio-group；G4 全量 21 处并补登设计规范 §2.5 词表）；**AutoComplete 严格选项模式纳入**并登记为 M3-5。
- [2026-09-20-m1-3-style-on-demand-landing.md](./2026-09-20-m1-3-style-on-demand-landing.md)：M1-3 样式按需形态落地与适配（Phase 11）。**结论**：`unbundle + css.inject` 落地——`tsdown.config.ts`、`exports`（`./theme.css` 取代 `./styles.css`）、resolver 与 Nuxt 模块注入基础层、`check:build` / `check:nuxt` 断言与文档口径同步（含架构 §4 决策反转留痕）。`pnpm verify` **exit 0**（1384 tests）；`npm pack` 341 文件 / 750.5 kB unpacked（基线 11 / 676 kB）。消费侧**真实包布局**实测：仅根导入 7,536 B（**不含基础层**）、补 `theme.css` 后 13,176 B（= 组件样式 + 基础层，**只有一份**）。**自纠一处**：M1-1 / M1-2 曾称「Vite 侧根导入会携带 tokens」为误，已就地更正。已知限制：裸 Node ESM 不能直接加载产物（CSS import）、基础层必须显式提供。
- [2026-09-20-m1-2-entry-semantics-and-dts-verification.md](./2026-09-20-m1-2-entry-semantics-and-dts-verification.md)：M1-2 入口语义与 dts 验证（Phase 11）。**结论**：① 消费方 `tsc` 解析 unbundled `dts` **通过**（`bundler` 与 `node16` 双模式 exit 0，含负向对照与基线对照）；② 入口语义（`exports["./theme.css"]`、不提供单体全量聚合、预设随 theme 交付）**已获用户确认**（2026-09-20 指令「提交后继续推进」按建议值采纳 D1~D6）；③ 实测发现**基础层不随 JS 图携带**（限定为 Vite / rolldown 系与 Nuxt；esbuild 实测保留；M1-3 期以真实包布局更正）（`injectStyles: false` 下基础 token 在产物 CSS / HTML / JS 中全为 0 命中，而 `check:nuxt` 仍 exit 0），且双份注入与覆盖顺序敏感，故 `injectStyles` 语义应改为「注入 theme 入口」；`check:nuxt` 的事实缺口**只在 M1-3 双通道形态下暴露**（基线同源注入下不成立），补断言属范围增量，见记录 §6 **D6——已获用户确认（2026-09-20）并已在 M1-3 落地**。
- [2026-09-20-m1-1-build-path-poc.md](./2026-09-20-m1-1-build-path-poc.md)：M1-1 构建路径 POC（Phase 11 样式分层拆分）。**结论：条件性可行**（待验：消费方 `dts` 解析与入口语义由 M1-2 消除、`exports` / resolver / Nuxt 模块 / `check-build` **四处适配**的落地复验由 M1-3 承担），推荐 **`unbundle: true` + `css.inject: true`**；Vite 侧消费实测「仅 `CaomeiButton`」产物 CSS 7,536 B / **1.48 KB gzip**、「Button + DataTable + DatePicker」33,616 B / **6.22 KB gzip**，对照全量 167,585 B / 25.60 KB gzip（收益随用量面变化，覆盖面广的下游接近全量）。**推翻「逐组件样式入口」实现假设**——模块图 + `sideEffects` 即可让消费方 tree-shaking 达成按需（依赖仅限遵守该语义的打包器，仅 Vite / rolldown 系实测）；多入口 + `css.splitting` 因文件名哈希、chunk 组成不可控、`index.css` 仍占 85% 而**不推荐**。破坏面：`exports` / `check:build` / **resolver 与 Nuxt 模块注入目标**（均为实测或代码判读，适配未实现）；`docs:build` 不受影响；`dts` 转为桶 + 逐模块。**§7 的 5 项范围调整（含取消 M1-4）已获用户确认（2026-09-20）**，`todo.md` M1 条目表与 `backlog.md` 迁出标注已同步改写（M1-2 待验项消除 + 入口语义落定、M1-3 落地与四处适配），M1-2 可启动。经 `@code-reviewer` Review Gate 两轮（R1 Reject → R2 Pass）。
- [2026-09-20-next-stage-scope-evaluation.md](./2026-09-20-next-stage-scope-evaluation.md)：下一阶段范围评估（候选盘点与阶段组合建议）。**结论**：候选充足但方向分散，按 [规划规范 §6](../../standards/planning.md) 收敛为 3–6 条主线；含 T1~T6 六组候选（覆盖 [Backlog](../../plan/backlog.md) 在册 59 条中的可纳入项：候选表逐项标注来源 / 规模 / 风险 / 依赖，条件触发表给判定口径）与 X / Y / Z 三个组合。**用户裁定（2026-09-20）**：取 **X + Y 混合**（不取 Z），两条修正——① **覆盖率不进入日常 `test` / `verify` 门禁**（现状约 90%，改为仅在周期性回归或 release 门禁校验）；② **功能开发优先**，CSS 分层拆分与相应组件优化先行。据此产出 §6 登记范围（4 条主线 / 14 条条目：M1 CSS 分层拆分含 POC、M2 重量级组件优化与样式治理、M3 组件能力增强、M4 质量门与文档守卫）与不纳入项（a11y 回归 / E2E 入门禁 / flaky / 文档站方向 / Phase 8 / momei 侧迁移）。**D3 已授权启动（2026-09-20）：已按 [规划规范 §4](../../standards/planning.md) 分配 Phase 11，并同步 [todo.md](../../plan/todo.md) / [roadmap.md](../../plan/roadmap.md) / [backlog.md](../../plan/backlog.md)**；M3 按现有取证直接做、不等 momei 反馈。
- [2026-09-20-css-on-demand-evaluation.md](./2026-09-20-css-on-demand-evaluation.md)：CSS 按需引入评估（`dist/styles.css` 单文件）。**结论**：用户观察成立（JS 侧可 tree-shaking、CSS 侧始终全量），判定**未命中插队例外**→ 登记 [Backlog §1.6](../../plan/backlog.md)。含现状取证（单文件 167,585 字节 / gzip 25.60 KB、`exports` 仅 `./styles.css`、resolver 固定注入整份、tsdown `css.splitting` 默认 `false`）、收益测算（基础层 1.14 KB / 预设 0.63~0.70 KB / 基础层 + 10 组件 ≈ 6.99 KB gzip）、组件间样式依赖实测（8 组）、主流库调研（Element Plus / Vant / Ant Design Vue 逐组件样式入口 + resolver `importStyle`；PrimeVue 走 base + preset）与四个候选方案。**用户 5 项裁定（2026-09-20）**：暂不启动（不紧急）／**不做逐组件入口**，收敛为「分层拆分」（`theme.css` + `presets/*.css` 优先，次批拆无上游依赖的 7 个重量级组件，依赖闭包类延后）／兼容性不作要求、下游自行修复／待新的需求评估再进待办。**启动后实施前须先交付构建路径 POC**。
- [2026-09-19-post-release-phase-implementation-audit.md](./2026-09-19-post-release-phase-implementation-audit.md)：发布后阶段规划落地复核（Phase 0 ~ Phase 10 与 Phase 5 第二阶段）。**结论**：所有已完成阶段的规划条目均已交付归档，未发现静默遗漏；未做项均为已登记类别（Phase 8 未启动、momei 侧迁移由外部执行、条件触发候选、[设计规范 §7](../design-spec.md) 明确不实现项、Backlog 待决策候选）。含逐阶段落地表与未做项汇总。
- [2026-09-19-phase5-first-release-execution.md](./2026-09-19-phase5-first-release-execution.md)：0.1.0 首发执行与发布后校验（Phase 5 第二阶段 F5-3 / F5-4）。**结论**：首发成功——npm `latest = 0.1.0`、tarball 11 文件与 `files` 一致、干净目录安装与子路径导入（`caomei-ui` / `caomei-ui/resolver` / `styles.css` / `THIRD-PARTY-LICENSES`）冒烟通过；F5-3 达成，F5-4（README / roadmap / guides 状态同步，无「未发布」残留）达成；npm `0.0.0` 占位经用户决策保留不 deprecate。含 registry 取证口径、发布后同步清单与未覆盖边界。
- [2026-09-19-phase5-second-stage-release-evaluation.md](./2026-09-19-phase5-second-stage-release-evaluation.md)：Phase 5 第二阶段发版评估（首版发布：本地手动发布，0.x）。**结论**：技术质量面已就绪（`pnpm verify` 全链路通过、`npm pack` 内容完整、子路径导出与许可合规），发布链路未就绪（npm 凭据缺失、semantic-release 首版恒 1.0.0）；**该结论为发布前评估快照，首版已于 2026-09-19 完成本地手动发布，见[首发执行记录](./2026-09-19-phase5-first-release-execution.md)**。含发版就绪度取证（HEAD `d3b85c8`）、0.x 首发机制（手工发布 + annotated 基线 tag `v0.1.0`；轻量 tag + `--follow-tags` 不会推送）、用户 4 项决策（授权启动 / 首版 0.x / 本地手动发布且 CI 自动发布暂缓 / 下游接入验证后置）、范围与非目标、风险与后续；条目登记见[待办事项](../../plan/todo.md) 当前阶段。
- [2026-09-14-momei-usage-audit.md](./2026-09-14-momei-usage-audit.md)：momei 组件使用复核台账（Phase 6 M1 交付物，需求 2 输入）。
- [2026-09-14-phase7-first-stage-evaluation.md](./2026-09-14-phase7-first-stage-evaluation.md)：Phase 7 第一阶段评估记录（迁移就绪范围、缺口优先级、i18n 注入机制与用户决策；该阶段已于 2026-09-16 完成并归档，交付见 [待办归档](../../plan/todo-archive.md)）。
- [2026-09-16-new-requirements-evaluation.md](./2026-09-16-new-requirements-evaluation.md)：2026-09-16 新需求评估记录（文档站信息架构 / 默认主色 / 站点观感 / Drawer 动画诊断 / 复用抽取 / 类型与 ESLint 严格化）。
- [2026-09-16-pre-release-stage-evaluation.md](./2026-09-16-pre-release-stage-evaluation.md)：下一阶段评估记录（发布前收口：文档站与代码质量；5 条主线、验收标准、容量与风险，未构成阶段登记）。
- [2026-09-16-m2-primary-browser-validation.md](./2026-09-16-m2-primary-browser-validation.md)：M2 条目 1「默认主色改蓝」浏览器验证记录（Phase 9 V 阶段：Reject 轮暗色 ConfirmDialog danger 4.07:1 回归；复验轮 F1 修复后 4.83:1、回归扫描无新增 → Pass，亮色 soft 变体 4.37:1 既有未达标维持 Backlog）。
- [2026-09-16-language-matrix-midterm-evaluation.md](./2026-09-16-language-matrix-midterm-evaluation.md)：语言矩阵 - 中期（zh-TW / ja-JP / ko-KR）范围评估——现状调研（22 命名空间 / 59 条文案 / 25 个 `.vue`（23 个组件目录）消费 / 基准回退语义与测试耦合点）、五个方案（A–E）比较与推荐、条目拆分、规模与风险、5 项待决策、包体实测与 tree-shaking 结论。**已获用户决策并登记为 Phase 10 M1（2026-09-16 授权启动）**。
- [2026-09-16-phase9-closure.md](./2026-09-16-phase9-closure.md)：Phase 9 收口与遗留清单——汇总 M1~M5 交付结果、契约变更裁定（含下游调研证据）、长期任务下一轮范围、下一阶段方向（已登记为 Phase 10）、已登记待执行项与已知偏差。
- [2026-09-16-m3-demo-motion-validation.md](./2026-09-16-m3-demo-motion-validation.md)：M3 条目 1「demo 动画恢复层」浏览器验证记录（Phase 9 V 阶段，共三轮）。首轮：最小验收通过——reduced-motion 下 demo 内 Drawer 入场 0.2s + 逐帧位移 13 位置、demo 外仍 1ms，Accordion/Image/Button/AutoComplete 与 4 个 Portal 面板入场实测播放，no-preference 下规则全部位于 reduce 媒体查询内；失败 2 项同一根因 P1＝Drawer / Accordion 的 `closed` 条目被 Reka `usePresence` 同名动画判定绕过（非回归）。复验轮（P1 修复后）：P1 闭环，但新发现 P2＝reduce 下 Accordion 高度插值仅「起始展开 item 的第一次折叠」成立（`--reka-collapsible-content-height` 被写死 `0px`）→ Reject。**末轮（P2 修复后，现行结论）：Pass——P2 闭环**，Accordion 改为「`animation-name` 更高特异性非 `!important` + duration/timing/iteration `!important`」后，Reka 测量窗口的内联 `animation-name: none` 恢复生效、变量在 `animationstart` 前写入自然高度，两条路径共 12 次过渡全部逐帧插值（distinctHeights 13~14）；P1 与 Portal / 加载指示未回归，CSSOM 逐规则归属与优先级断言成立 → **核对项 18 / 失败 0 / 观察项 6 / console 0**。
- [2026-09-17-m2-batch3-calendar-baseline.md](./2026-09-17-m2-batch3-calendar-baseline.md)：M2 批次 3「含日历面板（DatePicker / Calendar）」改动前基线与收敛记录——面板内容定宽 224×239（单月），改动前横向靠 floating-ui shift 落在视口内（390 右缘 0.18px 亚像素越界）、**纵向在极矮视口越出 13 / 53px**；补「可用空间上限（宽 + 高）+ 滚动降级」后 390 / 768 / 1280 与 320 / 280 / 240 探针几何逐项不变（仅上限制与溢出计算值变化），极窄 / 极矮探针下面板收敛且内容可滚动可达，常驻用例 48/48（含两个上限生效路径探针）；含桌面 1280 计算样式快照（断言 4 基线参考）与「内联日历内容宽 198px 在布局视口或容器宽 < 198px 时撑出溢出」的已知边界。
- [2026-09-17-momei-migration-feasibility.md](./2026-09-17-momei-migration-feasibility.md)：momei 迁移可行性评估（Phase 10 阶段尾部再评估点，2026-09-17）。**结论：可行（有条件）**——能力面无阻塞（台账 §4.1 需新组件 11/11 已交付；§4.2 受检 24 项＝完全交付 8 / 部分交付 16 / 完全未交付 0；momei 用量自 2026-09-14 逐项未变：59 组件 / 1515 用法 / 148 个 `.vue`）；三个先决条件是「DataTable 列级插槽先行（组件能力面唯一结构性差距：`<Column>` 153 / `#body` 123 / `slotProps` 163）、主题映射表与视觉基线先行（`--p-*` token 1394 处 + 20 个 SCSS + class 190 处）、分批 + 关键路径先行 + 并存白名单」。**用户决策（2026-09-17，5 项）**：按 **C3 分批全量**执行、**B1 库侧补齐先行**、B 级 **14 项全部纳入执行**（**范围登记，非「已交付」**）、**接受** 16 条有意差异、回归强度由**每周回归任务跑 momei 的测试**承载。含剩余缺口分级清单、迁移面量化（图标 598 / 134 文件、命令式 43 个 `.vue`、测试 21 文件）、16 条有意差异影响、C1/C2/C3 方案对照与五批分期建议；执行范围（B0~B4）见[路线图 Phase 7 第二阶段](../../plan/roadmap.md)与[本记录 §6](./2026-09-17-momei-migration-feasibility.md)（该阶段已于 2026-09-19 完成并归档，交付见[待办归档](../../plan/todo-archive.md)）。
- [2026-09-17-momei-migration-handover-plan.md](./2026-09-17-momei-migration-handover-plan.md)：momei 迁移计划与验收标准（Phase 7 第二阶段 M1 先行交付，2026-09-17）。本仓侧交付物，**作为对 momei 侧迁移结果的校验依据**；含批次划分与执行主体（B0a 库侧资产 / B1 库侧补齐由 caomei-ui 承担；B0b 视觉基线 / B2 / B3 / B4 由 momei 项目在其仓库执行）、前置条件、开工顺序、每批出口条件、16 条有意差异逐条核对清单（与[可行性评估 §4](./2026-09-17-momei-migration-feasibility.md) 一一对应）、视觉基线采集方法与判定口径（本仓不代为执行）、回归口径（由 momei 每周回归任务承载）、包体对比要求、逐批「文件 → 改动点」清单产出要求。原子条目登记与交付见[待办归档](../../plan/todo-archive.md)（Phase 7 第二阶段，2026-09-19 归档）。
- [2026-09-17-momei-migration-assets.md](./2026-09-17-momei-migration-assets.md)：momei 迁移 B0a 库侧资产（Phase 7 第二阶段 M2-1 / M2-2 / M2-3，2026-09-17）。含取证口径与可复现命令（快照 momei `cb663aee`、排除项、源码判读性质）与[评估记录 §2.3](./2026-09-17-momei-migration-feasibility.md) 的口径对照（差异已逐项列出，含 8 组扫描条件的复现尝试留痕，见该文档 §1.3 / §1.4；**2026-09-17 用户裁定以本仓扫描口径与实际结果为准**）；`--p-*` → `--caomei-*` 全量对照表（114 个唯一 token，语义优先 / 色阶不做等比平移 / 组件内部 token 不迁移 / 派生档位用 `color-mix`）；`pi pi-*` → `@lucide/vue` 全量映射表（128 个图标：同名直取 68、语义改名 48、填充变体 1、品牌图标无对应 11）；品牌图标候选方案；双库并存隔离策略（按路由前缀为最小切换单位、白名单载体归 momei 侧）与包体监控记录项与命令口径（与交接文档 §9 一致）。
- [2026-09-17-batch23-token-equivalence-ui-validation.md](./2026-09-17-batch23-token-equivalence-ui-validation.md)：批次 2（阴影 / 遮罩 token）与批次 3（禁用态不透明度 token，工作区未提交 diff）的**值等价**浏览器验证（V 阶段）。结论：**通过**——夹具 122 项 / 文档站交叉 19 项 / 宿主稳定性 12 项均无失败，console / pageerror / HTTP 0；7 个 token 原始值与历史字面量逐字一致，批次 3 全部 18 处迁移点与 `0.5` 档位抽样（11/19）逐一实测，亮 / 暗 / `data-preset="caomei"` 三态下阴影与遮罩计算值不变，批次 1 空 `label` 不渲染 / 不覆盖透传值成立；2 项观察项（Button 显式 `label` 与透传 `aria-label` 优先级分歧、AutoComplete 面板 `color-mix` 不在迁移范围）不计为缺陷。
- [2026-09-17-batch24-disabled-opacity-merge-ui-validation.md](./2026-09-17-batch24-disabled-opacity-merge-ui-validation.md)：禁用态不透明度 `0.5` 档归并（19 处 / 18 文件，工作区未提交 diff）的**有意视觉变更**浏览器验证（V 阶段，用户裁决「归并」）。结论：**通过**——后验夹具 114 项 + 响应式 / 宿主稳定性 / token 覆盖 / 可聚焦性 20 项、基线 `b7d9613` 对照 37 项，全部失败 0、console / pageerror / HTTP 0；19 处选择器「改动前 `0.5` → 改动后 `0.6`」逐项实测一一对应（含需开面板的条目项），前批 18 处 `0.6` 与 `skeleton` pulse 关键帧 `0.5` 未被波及，亮 / 暗 / 预设三态与阴影 / 遮罩、4 组件 `aria-label` 优先级契约无回归；3 项观察项（任务描述「wave 关键帧」应为「pulse 关键帧」、本轮无头 Chromium 滚动条宽 0、Vite 陈旧 transform）不计为缺陷。
- [2026-09-17-label-priority-unification-ui-validation.md](./2026-09-17-label-priority-unification-ui-validation.md)：label 优先级全库统一（11 组件 + Button / Badge / FileUpload，工作区未提交 diff）的**真实浏览器计算可访问名**验证（V 阶段）。结论：**通过**——夹具 51 项核对全过，失败 0，console error / pageerror / HTTP 0；三档契约「显式 `label` > 透传 `aria-label` > 语言兜底文案」由 `getByRole(role,{name})` + CDP `getPartialAXTree` 双证据逐组件实测，Calendar 无显式名时保留 `日历, 2026年9月` 月份上下文、Toast 视口名只落 `role="region"`（内层 `<ol>` 不重复）、暗色 + en-US 三组件兜底、禁用态 `0.6` / 阴影 / 遮罩计算值与 Button / Badge 优先级无回归；3 项观察项（SplitButton 组根同名、文档站 demo 未覆盖「只传 `aria-label`」、Vite 陈旧 transform）不计为缺陷。
- [2026-09-17-column-slots-ui-validation.md](./2026-09-17-column-slots-ui-validation.md)：DataTable 列插槽（Phase 7 第二阶段 M3，工作区未提交 diff）的**真实浏览器验证**（V 阶段，首轮 + 复验轮）。结论：**通过**——中英文档页「列插槽 / Column slots」示例首轮核对 48 项全过、失败 0、观察项 2、console error / pageerror / HTTP ≥ 400 均 0；`#cell-status` 渲染 `CaomeiTag`（primary/success，含亮暗对比度 5.56:1 / 7.16:1）、`#header-owner` 为 `<em>` 且非排序按钮、`title` 无插槽列仍走默认路径且点击排序 `aria-sort` `none→ascending`、移动 390 / 平板 834 无横向溢出、CLS 0.0136 归因 VitePress 主题 hydrate（`button` 页同值复现）。**复验轮（O1 示例修正后）25 项全过、失败 0、console 0**：`#cell-owner` 已去掉 `index + 1` 序号，中英页实测 `张三 / 李四`（`Alice / Bob`）且排序后每行仍为自身 owner，**O1 已消解**；站点级 CLS 观察项 O2 维持。视觉通道不可用，仅 OCR 文字层复核，**未做逐像素比对**。
- [2026-09-17-m6-report-defects-ui-validation.md](./2026-09-17-m6-report-defects-ui-validation.md)：M6 批次（用户缺陷报告触发的等待期修复，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——A~F 六组 **68 / 68 核对项通过、失败 0**，console error / pageerror / HTTP ≥ 400 均为 0。M6-1 圆角：既有「与选择器组合」demo 与 DOM 克隆合成的横向末成员 / 三成员中间 / 纵向首成员位次下，成员根 `.caomei-select__field` 与内层 `.caomei-select` 四角逐角一致（`8px 0px 0px 8px` / `0px 8px 8px 0px` / `0px 0px 0px 0px` / `8px 8px 0px 0px`），`--caomei-input-group-radius: 4px` 覆盖态两者同为 `4px 0px 0px 4px`，独立 `Select` 保持 `8px 8px 8px 8px` 且 `max-width` 仍 `320px`；M6-3 宽度：组内 `Select` 成员 `flex: 1 1 auto` / `max-width: none` / `593px`，触发器铺满包装层、末成员右边界与组右边界同为 `1043.5`（真实滚动条 15px 口径；隐藏滚动条下为已知事实的 `1051`）；M6-2 图标页中英各 4 段 live demo、预览区 SVG `7 / 7 / 24 / 4` 且可见、`aria-hidden` 全为 `true`。三档视口（1440 / 1024 / 375）与亮 / 暗均无横向溢出、拼接外观一致；浮层稳定性 `in-flow` 位移 0 / 滚动条容差内 / CLS 0（Select 无遮罩，判据 N/A）；4 项观察项（移动 2 列网格、Select 无遮罩、滚动条口径差、聚焦前 `position` 已 `relative`）不计为缺陷。视觉通道不可用，仅几何 / 计算样式 / aria 断言，截图落 gitignored 目录供人工复核。
- [2026-09-17-m6-4-m6-5-ui-validation.md](./2026-09-17-m6-4-m6-5-ui-validation.md)：M6 批次第二批（M6-4 图标一览卡片排版修复 + M6-5 `DatePicker` 触发按钮默认宽度上限，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——A~E 五组 **125 / 125 核对项通过、失败 0**，console error / pageerror / HTTP ≥ 400 均为 0。M6-4：CSSOM 实测根因规则 `.vp-doc li + li` 带 `margin-top: 8px`，修复后卡片计算 `margin: 0px`；中英 × 三档视口全部「同行卡片高度与顶边逐项一致」，全局仅一种卡片高 `102px`（桌面 5 列 / 122.8px、平板 4 列 / 135.75px、移动 2 列 / 131px），网格与页面无横向溢出；刀刃项移动 375 卡片内盒 `113px`、最长图标名 `TriangleAlert` 文本 `93.63px`（`scrollWidth 94 / clientWidth 94`），24 / 24 项无溢出；亮 / 暗边框与文本色均可辨。M6-5：默认 `width / max-width = 320px / 320px`（父容器 `646px`，回退 `--caomei-select-max-width = 20rem`），覆盖 `none` → `646px`、覆盖 `240px` → `240px`，移除后回到 `320px`；三档视口触发宽 `min(20rem, 可用宽)`（320 / 320 / 270），面板 portal 稳定（`in-flow` 位移 0、clientWidth `1425 → 1425`、CLS 0、Esc 可关），中英 token 表与生效变量一致；InputGroup × `DatePicker` 克隆夹具（注入两组件 dev 注入样式 + 独立态保真度自检）逐句核对新增文档条目**口径一致**，位次圆角 `8px 0px 0px 8px` / 全 0 / `0px 8px 8px 0px` 与 `-1px` 重叠成立；既有 InputGroup demo 几何与上一批 V 记录逐项一致（组 646 / Select 593 / 右边界 1043.5）。既有 `DatePicker` 单元用例 29 / 29 通过。5 项观察项（移动 2 列网格、面板无遮罩、面板 222px 窄于触发器、`AutoComplete` 亦不在铺满清单、移动卡片内盒 113px）不计为缺陷。
- [2026-09-18-m4-b1-data-form-ui-validation.md](./2026-09-18-m4-b1-data-form-ui-validation.md)：M4「B1 增强·数据与表单类」批次（MultiSelect 清除 + `#option` 插槽 / Paginator 每页条数 / Button 角标 / ToggleButton 两态文案，工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——A~G 七组 **119 / 119 核对项通过、失败 0**（A 22 / B 8 / C 28 / D 28 / E 16 / F 12 / G 5），console error / pageerror / HTTP ≥ 400 均为 0，观察项 3 不计为缺陷。MultiSelect 清除按钮 `aria-label` 中英「清除 / Clear」，压窄至 90px 后标签分两行（顶边 `[1273,1296]`）、清除按钮 14×14 与标签 rect 零相交且点击后模型清空、焦点回 `caomei-multi-select__input`、按钮移除；`#option` 面板 3/3 项渲染 SVG + `.demo-option`（非纯文本回退）。Paginator 选择器 client rect `96px`（`6rem`）、可访问名「每页条数 / Rows per page」，偏移保持实测 第 3 页 `21–30` → 每页 20 落第 2 页 `21–40 / 96` → 每页 50 落第 1 页 `1–50 / 96`，三档视口（1440 / 1024 / 375）分页器与页面均无横向溢出（646 / 567 / 270、1425 / 1009 / 360）。Button 角标点击 Add 后 `Notifications` 出现、内容与计数一致（1、3），外扩越过按钮 `top 423<432` / `right 585.23>576.23`，祖先 `overflow` 全 visible、完整落在预览容器内、`pointer-events:none`，出现前后 `.demo-row` clientWidth 646→646、按钮 delta 0、CLS 0，`neutral rgb(82,82,91)` vs `danger rgb(220,38,38)` 可辨。ToggleButton 可见文本与可访问名同步 `固定↔已固定` / `Pin↔Pinned`、`aria-pressed` false↔true。`showClear + disabled` 分支由一次性 Vite 夹具另测 5 / 5；M4-6 `Switch` 仅新增 `change` 事件、无渲染面变化，**显式跳过**。视觉理解通道不可用，仅 RapidOCR 文字层 + 几何 / 计算样式 / ARIA 断言。
- [2026-09-18-m4-4-m4-5-checkbox-group-datatable-ui-validation.md](./2026-09-18-m4-4-m4-5-checkbox-group-datatable-ui-validation.md)：M4-4 `Checkbox` 数组模型 + M4-5 新增 `CaomeiCheckboxGroup` 与 `DataTable` `rowsPerPageOptions`（工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——文档站 112 项 + 一次性夹具 18 项 = **130 / 130 通过、失败 0**（A 22 / B 24 / C 12 / D 44 / E 10 / 夹具 18），console error / pageerror / HTTP ≥ 400 均为 0，观察项 3 不计为缺陷。CheckboxGroup：`role="group"` + `aria-label`（水果 / Fruits）、4 选项含 1 禁用、点击按 value 增删（`已选：apple、banana`）、全选初始 `mixed` → 点击「已选 3 项」（榴莲不计入）→ 再点「已选 0 项」、榴莲恒 false；暗色选中背景 `rgb(96,165,250)`、禁用不透明度 `0.6`。Checkbox 数组模型：点击香蕉 / 苹果后文本 `已选：apple、banana` → `已选：banana`。DataTable：选择器宽 96px、可访问名「每页条数 / Rows per page」，偏移保持实测第 3 页（每页 5，偏移 10）切每页 10 → 落第 2 页且首行「成员 11」，三档视口（1440 / 1024 / 375）分页器 276 / 276 / 270 与页面 1425 / 1009 / 360 均无横向溢出。夹具覆盖 `name="fruits"` + `required`（选中项生成 `fruits[0]`/`fruits[1]`，空选 `checkValidity()` false）、`rovingFocus` 开 / 关 Tab 与方向键、全禁用下全选禁用、`invalid` 下发 `aria-invalid`；键盘焦点实测 2px solid 主色描边。视觉理解通道不可用，仅 RapidOCR 文字层 + 几何 / 计算样式 / ARIA / 键盘断言。
- [2026-09-18-m5-b1-dialog-ui-validation.md](./2026-09-18-m5-b1-dialog-ui-validation.md)：M5 批次 B1「Dialog 头部契约与断点宽度」（M5-1 `showHeader` / `title` 可选化 / `@hide` + M5-2 `breakpoints`，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——常驻 E2E **54 / 54**（含本批新增「浮层断点宽度」「无头部对话框」× 三档视口）、一次性实测核对 **45 / 45**、断言 4 基线 **54 项逐字段零差异**；console error / pageerror / 崩溃 0，无横向溢出。断点实测 390 → 370.5px（95vw，窄档胜出静态 `calc(100vw-32px)` 的 358px，证明注入规则源序 tie-break 生效）、768 → 652.8px（85vw）、1280 → 640px（回退 `lg`）；无头部档头部 / 关闭按钮计数 0、隐藏标题 `1px / inset(50%)`、可访问名与 `aria-describedby` 均可解析；宿主稳定性 `clientWidth` 不变 / `in-flow` 位移 0 / CLS 0；暗色 `rgb(11,11,13)` / `rgb(245,245,245)`。附带环境修复：root 容器 Chromium zygote 致交互崩溃，`--no-zygote` 恢复且保留多 context，口径同步测试规范 §7。
- [2026-09-18-m5-b1-dialog-baseline.md](./2026-09-18-m5-b1-dialog-baseline.md)：M5 批次 B1 改动前基线（[响应式设计 §4](../responsive.md) 断言 4）——基线 `37da558`（独立 worktree）与改动后逐视口比较默认对话框 18 个几何 / 计算样式字段，390 / 768 / 1280 三档共 54 项零差异；含断点三档实测表、层叠承重路径判别点、已知边界与复现命令。
- [2026-09-18-m5-b2-confirm-icon-popover-ui-validation.md](./2026-09-18-m5-b2-confirm-icon-popover-ui-validation.md)：M5 批次 B2「ConfirmDialog 图标 + Popover 声明式锚点」（M5-3 `useConfirm` 的 `icon` + M5-4 命令式收敛为声明式迁移写法与 `CaomeiPopoverTrigger` 的 `unstyled`，工作区未提交 diff）**真实浏览器验证**（V 阶段，文档站 Vite dev）。结论：**通过**——light / dark × 三态（默认 `Info` / danger `TriangleAlert` / 自定义 `Rocket`）实测图标类名与 `aria-hidden`、计算色、16px 尺寸、标题描述间距 12px（与改造前逐值一致）、几何断言与 0 console error，OCR 佐证暗色文案渲染；视觉推理通道不可用，未做像素级比对。
- [2026-09-18-m5-b3-dropdown-model-anchor-ui-validation.md](./2026-09-18-m5-b3-dropdown-model-anchor-ui-validation.md)：M5 批次 B3「DropdownMenu 项模型 + 声明式锚点」（M5-5 `model` 数据驱动项模型 + M5-6 `:popup` / `toggle(event)` 收敛为声明式锚点与 `CaomeiDropdownMenuTrigger` 的 `unstyled`，工作区未提交 diff）**真实浏览器验证**（V 阶段，文档站 Vite dev）。结论：**通过**——light / dark 实测项模型渲染（3 条目 + 1 分隔线 + 3 图标）、`command` 触发即关闭、面板经 Portal 挂载于 `body`、`data-side=bottom`、面板顶边＝触发器底边 + 4px（`sideOffset`）、左对齐、视口内、`as-child + unstyled` 下自定义按钮保留自身类且不含内建类、0 console error，OCR 佐证文案渲染。**判据收敛**：M5-6 原判据「`:popup` 生效有单测」无映射对象，经用户裁定（2026-09-18，采纳候选 A）收敛为「Portal 挂载于 `body` + `data-side`」单测与锚点几何实测，等价证据与处置见[待办归档](../../plan/todo-archive.md) 的 M5 段。视觉推理通道不可用，未做像素级比对。
- [2026-09-18-m6-6-m6-7-migration-docs-ui-validation.md](./2026-09-18-m6-6-m6-7-migration-docs-ui-validation.md)：M6-6 / M6-7「迁移文档体系」（组件页迁移节后置 + 中英《从 PrimeVue 迁移》专题页，工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——脚本 **75 / 75** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；四页（zh/en × data-table/paginator）三档视口实测「从 PrimeVue 迁移」为其后仅剩自动生成 `API` 标题的最后一个内容节；中英专题页 H1 / 五个章节 / 侧栏入口 / 设计规范 §7 链接齐备，入口表组件链接 ≥ 23 且覆盖 Dialog / Drawer；组件页迁移节 → 专题页真实点击跳转成功。观察项 2（en-US 文档页 @768 为既有 79px 横向溢出，未改动的 en 页同样命中、zh 页与 `en-US/plan/*` 正常）不计为缺陷。
- [2026-09-18-m6-8-migration-sections-ui-validation.md](./2026-09-18-m6-8-migration-sections-ui-validation.md)：M6-8 第一批「反馈与浮层 + 数据展示」10 个组件页中英迁移节（工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——脚本 **305 / 305** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；24 页（本批 20 + M6-7 回归 4）× 三档视口实测「从 PrimeVue 迁移」为 `<ComponentApi>` 前最后一个内容节；专题页入口表链接 ≥ 29 且覆盖两组全部组件。观察项 12（en-US @768 既有 79px 溢出）与 M6-6 / M6-7 记录同源。
- [2026-09-18-m6-8-group-a-ui-validation.md](./2026-09-18-m6-8-group-a-ui-validation.md)：M6-8 第二批「基础与布局」组迁移节（Avatar / Badge / Button / Card / Divider / Image / SplitButton / Tag，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——脚本 **493 / 493** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；40 页 × 三档视口迁移节位置、专题页入口表 ≥ 25、互链与 Image 错误态断言齐备。附带修复：Image error 示例由缺失路径改为不可解码 data URI，消除文档页的网络 404。观察项 20（en-US @768 既有溢出）。
- [2026-09-18-m6-8-group-b-ui-validation.md](./2026-09-18-m6-8-group-b-ui-validation.md)：M6-8 第三批「表单输入」组迁移节（Checkbox / CheckboxGroup / FileUpload / FloatLabel / Input / InputGroup / InputNumber / Password / RadioGroup / Slider / Switch / Textarea，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——脚本 **769 / 769** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；64 页 × 三档视口迁移节位置、专题页入口表、互链与 Image 错误态断言齐备。观察项 32（en-US @768 既有溢出）。
- [2026-09-18-m6-8-group-c-ui-validation.md](./2026-09-18-m6-8-group-c-ui-validation.md)：M6-8 第四批「选择器」组迁移节（AutoComplete / Calendar / ColorPicker / DatePicker / MultiSelect / Select / SelectButton / ToggleButton，工作区未提交 diff）**真实浏览器验证**（V 阶段）。结论：**通过**——脚本 **953 / 953** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；80 页 × 三档视口迁移节位置、专题页入口表、互链与 Image 错误态断言齐备。**M6-8 五个分组收口**：共 40 个组件页具备中英迁移节，未补节 6 页（Accordion / ButtonGroup / DropdownMenu / Stepper / Tabs / Toolbar；其中 DropdownMenu / Toolbar 待 M5 能力交付后回扫；ConfigProvider 无组件页）。观察项 40（en-US @768 既有溢出）。
- [2026-09-18-m5-b4-toolbar-zones-ui-validation.md](./2026-09-18-m5-b4-toolbar-zones-ui-validation.md)：M5 批次 B4（M5-7）「Toolbar 三分区插槽」（工作区未提交 diff）**真实浏览器验证**（V 阶段，文档站 Vite dev）。结论：**通过**——桌面 / 窄屏 390 / 暗色三态实测三分区按 `start → center → end` 渲染、`start` 贴左 / `end` 贴右 / 三区垂直居中（区中心偏差 `[0,0,0]`）、页面与工具条 0 横向溢出、0 console error；未使用分区插槽的默认路径零回归（无分区包裹层，根计算样式与改造前声明值逐项一致）。视觉推理通道不可用，未做像素级比对。
- [2026-09-18-m5-b5-progress-spinner-stroke-width-ui-validation.md](./2026-09-18-m5-b5-progress-spinner-stroke-width-ui-validation.md)：M5 批次 B5（M5-9）「ProgressSpinner `strokeWidth`」（工作区未提交 diff）**真实浏览器验证**（V 阶段，文档站 Vite dev）。结论：**通过**——不传时按 `size` 档位回退（`sm` / `md` 2px、`lg` 3px）；`1` / `3` / 静态属性 `stroke-width="4"`（字符串）/ `6` 全链路生效（内联变量与计算宽度逐项对应）；就地改写内联变量实测「字符串 `0.25rem` → 4px」「内联优先于档位类（`lg` 3px → 5px → 移除后回 3px）」与**负向对照**「原始非法值 `2` → `0px / none`（整条 border 被丢弃，圆环消失）」，后者即 Review Gate RG-B01 的证据基础（修复为白名单校验）；全部用例 `border-style: solid`、暗色一致、0 console error。视觉推理通道不可用，未做像素级比对。
- [2026-09-19-m5-8-image-preview-ui-validation.md](./2026-09-19-m5-8-image-preview-ui-validation.md)：M5 批次 B5（M5-8）「Image `preview` 点击放大与遮罩」（工作区未提交 diff）**真实浏览器验证**（V 阶段，一次性 Vite 夹具 + 本地 SVG 图源）。结论：**通过**——**33 / 33** 核对项通过、失败 0，console / pageerror 0；遮罩 `role="dialog"` + `aria-modal="true"`、完整覆盖视口 `0/0/1280/800`、`in-flow` 位移 0（`16→16` / `281→281`）、滚动锁 `body.paddingRight 15px` 对齐基线滚动条宽、关闭后宿主 `1265→1265`、CLS 0；`Escape` / 遮罩点击 / 关闭按钮 / 焦点陷阱与焦点回归、`Enter` 打开、`previewIcon` 与 `#indicatoricon` 替换、暗色 `rgba(0, 0, 0, 0.45)` / `rgb(255, 255, 255)` 均通过。过程发现：Reka `DialogContent` 不自动输出 `aria-modal`，V 阶段拦下并补齐（同步单测与夹具断言）。未覆盖文档站外部图源示例的运行时点击、触摸 / 移动端与多实例场景。
- [2026-09-19-m5-10-fileupload-ui-validation.md](./2026-09-19-m5-10-fileupload-ui-validation.md)：M5 批次 B5（M5-10）「FileUpload `mode` / `maxFileSize` / `auto` / `chooseLabel` + `customUpload` / `uploader`」（工作区未提交 diff）**真实浏览器验证**（V 阶段，一次性 Vite 夹具 + `setInputFiles` 内存文件）。结论：**通过**——**23 / 23** 核对项通过、失败 0，console / pageerror 0；advanced 列表 `a.png 512 B` / `b.png 2.0 KB` 与移除路径、basic 紧凑形态与 `chooseLabel`「选择并上传」、`customUpload` + `auto` 触发 `uploader`（demo 记录 `ok.png`）、`maxFileSize` 超限拒绝（`big.png 超过大小上限 1.0 KB`，`role="alert"`，不触发 `uploader`）、新选择清空提示、禁用态与键盘聚焦、暗色 token（`rgb(245,245,245)` / `rgb(11,11,13)`）、basic 交互区间 CLS `delta=0` 与页面无横向溢出均通过。非浮层组件，不适用测试规范 §5.1 遮罩 / 滚动锁测量，已登记口径。
- [2026-09-19-m6-8-accordion-ui-validation.md](./2026-09-19-m6-8-accordion-ui-validation.md)：M6-8 剩余 4 页之一「Accordion 折叠面板」中英迁移节（工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——脚本 **13 / 13** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；zh / en 组件页「从 PrimeVue 迁移」为其后仅剩自动生成 `API` 标题的最后一个内容节，迁移表含 `v-model:value` / `multiple` / `unmountOnHide` / `lazy` / `activeIndex` 映射与 `expandIcon` / `selectOnFocus` 未实现项且链到专题页；专题页「导航与操作」/「Navigation & Actions」行新增 Accordion 链接、未穷尽脚注已移除 Accordion。首轮 4 项失败为断言口径问题（未排除自动生成 `API` 标题、未扫引用块），修正后全过；本批零 `src/**` 改动，未做三档视口复测。
- [2026-09-19-m6-8-tabs-ui-validation.md](./2026-09-19-m6-8-tabs-ui-validation.md)：M6-8 剩余页之一「Tabs 选项卡」中英迁移节（工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——脚本 **13 / 13** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；zh / en 组件页「从 PrimeVue 迁移」为其后仅剩自动生成 `API` 标题的最后一个内容节，迁移表含 `v-model:value` / `unmountOnHide` / `lazy` / `selectOnFocus` / `activationMode` / `activeIndex` 映射与 `scrollable` / `showNavigators` / `#previcon` / `#nexticon` / `tabindex` / `asChild` / `headerStyle` 等未实现项且链到专题页；专题页「导航与操作」/「Navigation & Actions」行新增 Tabs 链接、未穷尽脚注已移除 Tabs。校验前自查修正 `activationMode` 被同时写成映射目标与本库新增项的自相矛盾；本批零 `src/**` 改动，未做三档视口复测。
- [2026-09-19-m6-8-closure-ui-validation.md](./2026-09-19-m6-8-closure-ui-validation.md)：M6-8 收口「ButtonGroup + Stepper 页迁移节」（工作区未提交 diff）**真实浏览器验证**（V 阶段，`docs:build` 产物 + `vitepress preview`）。结论：**通过**——脚本 **18 / 18** 项通过、失败 0、console / pageerror / HTTP ≥ 400 均为 0；ButtonGroup（无功能 props 的拼接容器 + `orientation` 本库新增 + `pt`/`dt`/`unstyled` 未暴露）与 Stepper（`v-model:value` 值域收窄为 1 基 `number`、`linear` 默认 `true`、`<StepPanels>`/`<StepPanel>` 面板容器未实现）的 zh / en 迁移节均为其后仅剩 `API` 的末节并链到专题页；专题页「基础与布局」/「导航与操作」两行补链接，未穷尽脚注示例清单清空。**M6-8 主线收口**：全部组件页具备中英迁移节。本批零 `src/**` 改动，未做三档视口复测。

- [experience-archive.md](./experience-archive.md)：Session 经验归档（已蒸馏条目的摘要与链接，跨机器留存）。
