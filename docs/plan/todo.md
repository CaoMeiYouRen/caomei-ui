# 待办事项

本文档记录当前阶段的原子条目与验收状态；准入、命名与阶段衔接规则见 [规划规范](../standards/planning.md)，本文档不重述。

## 当前阶段

### Phase 14：质量与一致性收口

- **授权**：2026-09-25 用户裁定 D1~D10（范围与取向）后登记（**登记 = 范围授权；实施未启动**，各条目按 PDTFC+ 逐条开工与提交）；编号按 [规划规范 §4](../standards/planning.md) 取已有最大编号 +1（已有最大为 Phase 13）。范围依据见[下一阶段范围评估](../design/governance/2026-09-25-next-stage-scope-evaluation.md) §10。
- **定位**：库内质量与一致性收口——取向为「质量与治理收口 + 组件能力与长尾」，**不依赖下游反馈即可独立闭环**。
- **目标**：① a11y 既有例外处置与受检面扩面（可按规范清零者落地并收窄例外清单）；② 样式与 token 一致性收口（审计出可复算清单 + 机检守卫 + 触发器契约归一）；③ 测试稳定性（flaky 根因定位并修）与 Review Gate 证据留存装置；④ 治理守卫精选并接入门禁链；⑤ 组件能力与长尾条件候选判定表。
- **非目标**：**不发布新版本**（D10）；**不改 token 色值**（D5 / D6 仅审计出清单）；不为凑数纳入零下游用量的组件能力（D9 只出判定表）；不改 Reka primitive；不启动 Phase 8；不做 dependfix / momei 侧迁移实施；不修改 `AGENTS.md`（受保护文件）。
- **用户决策（2026-09-25，对应评估记录 D1~D10）**：① **D1**：M1~M5 **全取**；② **D2**：a11y Toast 焦点哨兵**维持现状仅更新记录**；③ **D3**：受检面扩面**只纳入可稳定驱动者**；④ **D4**：字段 shell 共享取**语义 token 契约**；⑤ **D5**：对比度遗留项**仅盘点并登记、不改色**；⑥ **D6**：实底前景配对**仅出清单、修复另行裁定**；⑦ **D7**：flaky **定位根因并修**；⑧ **D8**：治理守卫**按待办剩余容量补足**；⑨ **D9**：组件能力与长尾**只出判定表、不纳入实现**；⑩ **D10**：**暂不发版**。
- **执行顺序**：M1 / M2 文件域不重叠、可并行；M3 / M4 / M5 文件域独立。同一主线内条目按本表自上而下顺序执行；跨主线改同一文件时串行。实施以「开工」指令为准。
- **并行长期任务**：[长期任务台账](./recurring.md) 的「字段 shell 样式层共享」批次形态已裁定（**语义 token 契约**，D4②），由该台账跟踪交付、**不占阶段原子条目**（[规划规范 §8](../standards/planning.md)）。
- **阶段验收**：阶段验收通则见[路线图 §4](./roadmap.md)；每个原子条目收尾必须经 `@code-reviewer` Review Gate，涉及界面 / 交互时另经 `@ui-validator` 验证；样式 / token 改动以计算样式取证装置（239 项基线）证明零漂移；本阶段**无发布动作**，阶段收口前执行一轮[长期任务](./recurring.md)门槛复核并留痕。

#### M1 a11y 例外处置与受检面扩面

- **状态**：4 条原子条目已全部交付（2026-09-26，提交 `7a77bd5` / `f6f5341` / `f95b715` / `2e75e53` / `eaff97c`，均未推送）。例外清单 3 → 1（仅剩 Toast 焦点哨兵，按 D2③ 维持现状）；12 个面板内导出与 DataTable 折叠 toggle 全部入受检面（逐项判定表与证据见[交付与验证记录](../design/governance/2026-09-26-m1-a11y-exception-disposal-and-surface.md)）。同批登记 2 项同类悬空引用待后续处置（[Backlog](./backlog.md)）。

- 执行范围：① 修复可按规范清零的例外——`CaomeiCalendar` 根容器 `aria-label` 落点、`CaomeiMultiSelect` 关闭态空 `aria-controls`、`CaomeiStepper` 悬空 `aria-describedby`；② 受检面扩面——逐项判定 12 个面板内导出与 DataTable 折叠分组 toggle 的**可稳定驱动性**，可驱动者入受检面（D3③）；③ `CaomeiToastProvider` 焦点哨兵例外按 D2③ **维持现状**，仅更新记录注记（不引入定向豁免机制）。
- 非目标：不改造 Reka primitive；不为扩面而扩面（不可稳定驱动者维持登记 + 触发点）；不改组件视觉；不改动既有 3 条例外中不属本主线的判定。
- 最小验收标准：① 修复项落地后**例外清单同步收窄**（`count` 指纹更新、条目删除须有等价新增断言）；② `pnpm test:a11y` 例外外零违规；③ 受检面集合与 `caomeiComponents` 的差集有据、穷尽性机检、不可驱动项有登记理由；④ 涉及引用型 ARIA 属性的判定以**真实 Chromium** 复验。

| 编号 | 条目 | 范围 | 最小验收标准 | 依赖 |
| :-: | --- | --- | --- | :-: |
| M1-1 | `CaomeiCalendar` 根容器 `aria-label` 落点修复 | 补显式 role 或把标签落到具备 role 的元素（择一，实施时定）；保留既有可访问名 | 例外清单该条删除或更新 count；`test:a11y` 例外外零违规；Chromium AX 树名称仍可解析 | D3③ |
| M1-2 | `CaomeiMultiSelect` 关闭态空 `aria-controls` | 关闭态不输出空引用，开启态指向正确面板 id 不变 | 关闭 / 开启两态断言；Chromium 两态复验；例外清单同步收窄 | D3③ |
| M1-3 | `CaomeiStepper` 悬空 `aria-describedby` | 无 `CaomeiStepperDescription` 的步骤条件输出该属性；有描述时不变 | 有 / 无描述两态断言；例外清单无新增；文档形态零影响 | D3③ |
| M1-4 | 受检面扩面（只纳入可稳定驱动者） | 逐项判定 12 个面板内导出与 DataTable 折叠分组 toggle 的可驱动性；可稳定驱动者入 axe 受检面，其余维持登记 + 触发点 | 受检面差集有据 + 穷尽性机检；不可驱动项有登记理由；新增受检项带断言 | D3③ |

#### M2 样式与 token 一致性收口

- **状态**：4 条原子条目已全部交付（2026-09-26，提交 `f64a63b` / `b4d0d0d` / `f0280a3` / `f8598d7` / `144cfcf`，均未推送）。**不改任何 token 色值与既有视觉**（D5 / D6）：M2-1 配对复核清单（五档 `-solid` × `on-solid` 全一致、禁令零违反）、M2-2 对比度盘点 6 项逐条复算（无新增、无修复消除）、M2-3 触发器 `disabled` 透传与包装层归一化（`capture:styles` 239 项 0 差异）、M2-4 一致性机检（样式选择器旧命名 + 规则/声明双下界）。待裁定 2 项（switch 配对 / toast 回退口径）、对比度在册缺口 5 项与流程缺口 1 项（capture 未随引用型属性改动复跑）均已登记 [Backlog](./backlog.md)。逐条清单与证据见[交付与验证记录](../design/governance/2026-09-26-m2-style-token-consistency.md)。

- 执行范围：① 实底前景 token 配对复核——枚举 `-solid` 实底与自适应底的配对消费点，**仅出可复算清单**（D6②，修复不并入本阶段）；② 对比度遗留项盘点——**仅盘点并登记、不改色**（D5①）；③ 触发器 `disabled` 透传与包装层归一化；④ 一致性机检——样式侧旧命名裸类守卫与 `check-design` 声明解析健壮性。
- 非目标：不做与一致性无关的样式重构；**不改任何 token 色值 / 既有视觉**（D5 / D6）；不纳入字段 shell 批次（由[长期任务台账](./recurring.md) 跟踪）。
- 最小验收标准：① 配对复核与对比度盘点输出**可复算清单**（逐条消费点 / 缺口 + 判定 + 快照日期），不改色；② 触发器 `disabled` 改动在计算样式基线上零漂移；③ 新增 / 扩围守卫带正反例语料 + 抗静默收窄 + 接入 `check:design`。

| 编号 | 条目 | 范围 | 最小验收标准 | 依赖 |
| :-: | --- | --- | --- | :-: |
| M2-1 | 实底前景 token 配对复核（仅出清单） | 枚举 `-solid` 实底与自适应底的配对消费点，输出清单（消费点 + 配对判定 + 结论） | 清单可复算（逐条源代码位置）；发现不达标项登记待裁定，**不修复**（D6②） | D6 |
| M2-2 | 对比度遗留项盘点（仅登记） | 复核 4 项既有缺口 + `caomei` 预设 `primary-solid` 例外 + 站点侧 `text-muted` 观察，输出盘点并登记 | 逐条带依据、消费点与快照日期；**不改色**（D5①） | D5 |
| M2-3 | 触发器 `disabled` 透传与包装层归一化 | date-picker / color-picker 向触发器透传 `disabled`；两个薄包装对 `false` / `undefined` 归一化对齐 | 两态断言；既有行为与计算样式零漂移 | — |
| M2-4 | 一致性机检（样式侧旧命名裸类 + `check-design` 解析健壮性） | 旧尺寸命名检测扩到样式选择器；`declarationsOf` 只把匹配属性名形态计为声明，属性名非自定义属性时 `toLowerCase()` 比较 | 正反例语料（含值内分号 / 大小写反例）+ 抗静默收窄；接入 `check:design` | — |

#### M3 测试稳定性与质量装置

- 执行范围：① flaky 根因定位与修复——就 4 次出现记录（`auto-complete` / `color-picker`）做可复现路径判定并根治（D7①）；② Review Gate 证据留存装置。
- 非目标：不引入与 flaky 无关的测试基建；不以「加 sleep」掩盖时序缺陷；不把浏览器依赖引入 `verify`。
- 最小验收标准：① flaky 结论可判定（给出复现命令与失败率口径，或明确「本地无法稳定复现」并升级跟踪）；② 装置入库并可从记录 / 门禁引用；③ 全量 `pnpm test` 连续多轮稳定。

| 编号 | 条目 | 范围 | 最小验收标准 | 依赖 |
| :-: | --- | --- | --- | :-: |
| M3-1 | flaky 根因定位与修复 | 4 次出现记录（`auto-complete` / `color-picker`）的并发配置 / 隔离边界 / timer 作用域判定与根治 | 复现命令 + 失败率口径；全量 `pnpm test` 连续多轮稳定；无法复现则给出升级依据 | D7 |
| M3-2 | Review Gate 证据留存装置 | `artifacts/review-gate/` 归档形态与索引；可从记录 / 门禁引用 | 装置入库 + 索引齐备；不引入浏览器依赖到 `verify` | — |

#### M4 治理守卫精选

- 执行范围：从评估记录 T4 五项中取**容量内 3 项**（D8）——内置文案台账机检对账、组件总览页与侧栏成员对账、迁移口径一致性守卫；其余 2 项（README / roadmap 版本句弱守卫、文档站主题 CSS 的 lint 覆盖）维持 Backlog。
- 非目标：不做与一致性无关的守卫扩面；不把超出容量的守卫纳入本阶段。
- 最小验收标准：守卫带正反例语料、抗静默收窄（受检面下界 + 空扫描拒绝 + 允许名单反向校验）、接入既有门禁链；存量不一致（中英总览页缺 `CheckboxGroup`）同批修复并由守卫兜底。

| 编号 | 条目 | 范围 | 最小验收标准 | 依赖 |
| :-: | --- | --- | --- | :-: |
| M4-1 | 内置文案台账机检对账 | `docs/components/locale.md`（中英）的「命名空间 → 文案键」索引表 ↔ `src/locale/*` 实际键集合对账 | 正反例 + 抗静默收窄；接入 `governance:check`；台账存量与实现对齐 | — |
| M4-2 | 组件总览页成员对账 | ① **中文总览页**（`/components/index.md`）：分组顺序 + 成员 ↔ 侧栏 / [文档与演示站 §11](../design/documentation-site.md)，纳入 `check-docs-structure` 受检面；② **英文总览页**（`/en-US/components/index.md`）：只校「已翻译组件条目 ↔ `docs/i18n/en-US/components/*.md` 集合 + 顺序」，**不校分组结构**（英文页按翻译覆盖组织，属 `check-i18n-parity` 的 `STRUCTURE_EXEMPTIONS` 已登记的有意差异）；同批修复两侧缺 `CheckboxGroup` | 守卫覆盖中文总览页分组与成员；英文侧按「已翻译页集合」校且与既有 parity 豁免口径一致、不产生假阳性；存量缺口修复后守卫兜底；`docs:check` 全绿 | — |
| M4-3 | 迁移口径一致性守卫 | 迁移指南「逐组件对照入口」表 / 组件页迁移节 ↔ [设计规范 §7](../design/design-spec.md) 集合对账 | 正反例 + 抗静默收窄；接入 `docs:check` 或 `governance:check`；不宣称穷尽 | — |

#### M5 组件能力与长尾判定

- 执行范围：对评估记录 T5 的条件触发 / 不补候选（DatePicker 范围选择、ColorPicker 色板导航、DataTable 滚动高度、Paginator 页码报表、Sidebar、ScrollPanel）与 `@iconify/vue` 逐条复核触发条件与依据，输出**判定表**。
- 非目标：**不纳入任何组件实现**（D9②）；不为凑数纳入零下游用量能力；不改组件代码。
- 最小验收标准：判定表逐条带触发条件、当前取证与结论（维持条件触发 / 不纳入 / 升级依据）；`@iconify/vue` 等维持 Backlog。

| 编号 | 条目 | 范围 | 最小验收标准 | 依赖 |
| :-: | --- | --- | --- | :-: |
| M5-1 | 条件候选判定表 | T5 条件候选 6 项 + `@iconify/vue` 逐条复核触发条件与依据 | 逐条带触发条件与取证；不纳入实现（D9②）；结论可复算 | D9 |

#### 不纳入本阶段（含依据）

| 项 | 依据 |
| :--- | :--- |
| dependfix `apps/platform` 迁移实施 / momei 侧迁移 | 执行主体为各自仓库；本仓等待反馈（用户裁定取向 1 暂不列入） |
| Phase 8 下游兼容性回归机制 | 用户裁定：维持未启动，等待相关项目反馈后再评估 |
| 修改 `AGENTS.md`（AI 资产指针候选） | 受保护文件，须用户明确指示 |
| 富文本与图表封装 | [Backlog](./backlog.md) §1.3 为**外购建议**（Tiptap / ECharts），非自研候选 |
| `ScrollPanel` / `Sidebar` 库侧补齐 | 既有裁定「不自研 / Drawer 承接」，下游评估已确认不构成阻塞 |
| 国际化长期矩阵 / RTL / locale 组织、移动端两项 | 长期候选，本取向未取，留 [Backlog](./backlog.md) |
| 文档站与工具链长期装置、README / roadmap 版本句弱守卫、文档站主题 CSS lint、直连 Reka 触发器守卫、`@iconify/vue` | 长期候选或 D8 / D9 未取，留 [Backlog](./backlog.md) |
| 对比度改色 / 实底前景配对修复 | D5 / D6 仅审计出清单，修复须另行裁定 |
| 新版本发布 | D10：暂不发版 |

## 未完成项汇总

> 本节仅汇总未完成项以供跨阶段可见，**不构成阶段待办登记**（登记需用户明确决策）。

- **进行中阶段**：Phase 14（质量与一致性收口，2026-09-25 登记；实施以「开工」指令为准）——范围见上节。
- **未启动 / 未完成阶段**：Phase 8（下游兼容性回归机制，稳定后启用）——用户 2026-09-23 裁定维持未启动，等 dependfix 完成迁移后再评估。范围见[路线图](./roadmap.md)。
- **等待外部反馈**：dependfix `apps/platform` 迁移实施（B0~B4）与 momei 侧迁移（本仓交接批次 B0b / B2 / B3 / B4；momei 侧第六十七阶段在飞）由对应仓库执行，本仓等待反馈。
- **后置项**：Phase 5 第二阶段的下游接入验证（发布后由下游实际迁移反馈驱动）。
- **未纳入任何阶段的候选**：见 [Backlog](./backlog.md)（组件增强、长尾组件、国际化与 RTL、移动端与响应式、基建与治理、服务层、下游协同等分组）。
- **已完成阶段的遗留项与已知偏差**：见[待办归档](./todo-archive.md) 与 [Backlog](./backlog.md)（后者承载其中仍待决策的候选）。
