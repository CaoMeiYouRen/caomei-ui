import type { CaomeiLocaleMessages } from './types'

const enUS: CaomeiLocaleMessages = {
    dialog: {
        close: 'Close',
    },
    input: {
        clear: 'Clear',
    },
}

export default enUS
