export interface CaomeiLocaleMessages {
    dialog: {
        close: string
    }
    input: {
        clear: string
    }
    inputNumber: {
        increase: string
        decrease: string
    }
    message: {
        close: string
    }
    pagination: {
        label: string
        first: string
        previous: string
        next: string
        last: string
        page: string
    }
    progress: {
        loading: string
    }
    tag: {
        close: string
    }
    table: {
        empty: string
    }
    toast: {
        label: string
        viewport: string
        close: string
    }
}
