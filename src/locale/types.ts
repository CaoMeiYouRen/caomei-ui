export interface CaomeiLocaleMessages {
    dialog: {
        close: string
    }
    input: {
        clear: string
    }
    inputNumber: {
        increase: string
        decrease: string
    }
}
