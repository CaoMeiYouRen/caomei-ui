export interface CaomeiLocaleMessages {
    dialog: {
        close: string
    }
    input: {
        clear: string
    }
    inputNumber: {
        increase: string
        decrease: string
    }
    tag: {
        close: string
    }
    table: {
        empty: string
    }
    toast: {
        label: string
        viewport: string
        close: string
    }
}
